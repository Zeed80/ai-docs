# Third-party test drawings (external images, not ours to redistribute — see
# test-drawings/SOURCES.md + fetch.sh for provenance/licenses and re-download)
test-drawings/*.jpg
test-drawings/*.jpeg
test-drawings/*.png
test-drawings/*.svg

# Real photos/DWG of company drawings for cleanup-mode testing (private data)
cleanup_test_files/

# Real photos of drawings for vectorize-mode testing (private data)
test_vector_files/

# Generated LoRA training data (tools/lora-dataset — regenerate, don't commit)
datasets/lora-cleanup*/

# Generated CAD neural-vectorizer training data (tools/cad-dataset — regenerate, don't commit)
cad-dataset-out/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
.ruff_cache/

# Node
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
playwright-report/
test-results/
frontend/playwright-report/
frontend/test-results/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
.claude/
.codex
.mcp.json

# OS
.DS_Store
Thumbs.db

# Docker
docker-compose.override.yml

# Data
datasets/*.pdf
datasets/*.json
!datasets/.gitkeep
data/
!backend/data/canvas_skill_map.json
*.db
*.sqlite
*.sqlite3
backend/celerybeat-schedule*
example-invoices/*
!example-invoices/.gitkeep
example-drawings/*
!example-drawings/.gitkeep

# OpenClaw local runtime, device identity, logs, and downloaded runtime deps
#aiagent/official/


# MinIO data
minio_data/

# Postgres data
postgres_data/

# Redis data
redis_data/

# Qdrant data
qdrant_data/

# Ollama models
ollama_data/

# Coverage
htmlcov/
.coverage
coverage.xml

# Pytest
.pytest_cache/

# Alembic
backend/app/db/versions/*.pyc
backend/data/**
!backend/data/canvas_skill_map.json
eval_results_*.json

# Local env backups / secrets
*.bak
infra/.env.dev.bak

# Real invoice samples (confidential data — used for local regression only)
example-invoices/
backend/example-invoices/

# Generated test/quality reports
backend/tests/reports/
# Server-side backup archives — root only (must NOT match frontend/app/admin/backups/)
/backups/

# Generated from routes.yml.template by install.sh/update.sh (contains the real domain)
infra/traefik/prod/routes.yml

# Local built APK artifact (do not commit)
/sveta-debug.apk
*.apk
