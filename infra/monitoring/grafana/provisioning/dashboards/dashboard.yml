apiVersion: 1

providers:
  - name: "AI Workspace"
    orgId: 1
    folder: "AI Workspace"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 60
    options:
      path: /etc/grafana/provisioning/dashboards
