# Traefik dynamic routing configuration for production.
# Replace example.com with the public application domain before mounting this file.
http:
  routers:
    frontend:
      rule: "Host(`example.com`)"
      service: frontend
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      priority: 1

    backend-api:
      rule: "Host(`example.com`) && PathPrefix(`/api`)"
      service: backend
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      priority: 100

    backend-ws:
      rule: "Host(`example.com`) && PathPrefix(`/ws`)"
      service: backend
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      priority: 100

    backend-health:
      rule: "Host(`example.com`) && Path(`/health`)"
      service: backend
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      priority: 100

    minio-console:
      rule: "Host(`minio.example.com`)"
      service: minio-console
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt

  services:
    frontend:
      loadBalancer:
        servers:
          - url: "http://frontend:3000"

    backend:
      loadBalancer:
        servers:
          - url: "http://backend:8000"

    minio-console:
      loadBalancer:
        servers:
          - url: "http://minio:9001"

  middlewares:
    security-headers:
      headers:
        stsSeconds: 31536000
        stsIncludeSubdomains: true
        contentTypeNosniff: true
        frameDeny: true
        browserXssFilter: true
        contentSecurityPolicy: "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; connect-src 'self' ws: wss:"

    rate-limit:
      rateLimit:
        average: 100
        burst: 50
