"""Синтетический корпус с эталоном геометрии для проверяльщиков оцифровки.

Реальных пар «чертёж + исходная 3D-модель» у предприятия нет, а корпуса, что
есть, эталона положения не несут: `ground_truth.json` хранит только значения
размеров, проекции STEP — голые рёбра без надписей. Здесь эталон строится по
построению: известный спек → тело в ядре → лист из тела (виды, разрезы,
размеры по ГОСТ) → растр. Генератор знает то, чего нет ни в одном корпусе:
к какой геометрии относится каждый размер и в каком виде лежит каждый элемент.
"""
