# Roadmap: автономная память, exploratory-задачи и self-learning connector library

## Контекст

Продолжение обсуждения архитектуры агента «Света» (см. критику агентской системы в этой же сессии). Пользователь хочет, чтобы агент был по-настоящему самостоятельным помощником инженера: умел долго и автономно работать над открытыми задачами (пример — «найди каталоги поставщиков»: агент сам решает, как искать, скачивает и структурирует что нашёл в SQL + векторно-графовую БД со связкой картинок инструмента, честно перечисляет, чего не нашёл), ориентировался на успех, дообучался по итогам своих действий, а не был скован жёсткими рамками там, где рамки не нужны для безопасности.

Ресёрч (3 параллельных Explore-агента + прямая проверка ключевых файлов) показал: большая часть нужной инфраструктуры **уже существует и не используется на полную**, а не отсутствует:

- `WorkOrder` уже поддерживает `parent_id` + `PlannedStep.kind="decompose"` — планировщик уже умеет fan-out дочерних WorkOrder с автоматическим сплитом бюджета (`_split_child_budgets`, `backend/app/tasks/work_orders.py:180-263`). Это готовый механизм для «разослать по N поставщикам параллельно», не нужно изобретать новый.
- `generate_capability_plan` уже принимает `completed_context`/`failure_context` и **явно запрещает модели повторять завершённую работу при реплане** (`backend/app/domain/work_planning.py:137-144`) — инкрементальный replanning-цикл уже есть, а не только «bounded replanning после провала».
- `WorkStepAttempt.checkpoint` (JSON) существует в схеме, но нигде не читается/не пишется в рантайме — готовая точка для инкрементального прогресса долгих шагов.
- `WorkOrder.budgets`/`constraints`/`metadata_` — JSON-поля без строгой схемы, значит бюджет по времени/запросам и флаг «exploratory-режим» можно добавить **без миграции**, просто новой конвенцией ключей.
- `WorkStepAttempt.tokens_used`/`cost_usd` уже трекаются по попытке — агрегация LLM-бюджета не требует нового счётчика.
- `ComputerUseGrant`/`ComputerUseAction` (allowlist хостов, лимит операций, аудит) — готовый, уже проверенный securit-механизм для веб-доступа; никакого отдельного «bypass» для скрапинга придумывать не нужно.
- `assert_completion_allowed`/`record_verifier_verdict` — существующий fail-closed паттерн приёмочных критериев (детерминированный + независимый semantic verifier) — «честное покрытие» можно выразить как ещё один критерий в этой же системе, не трогая её FSM.
- `ToolSupplierCreate/Out`, `ToolCatalogEntryCreate/Out` (`backend/app/domain/tool_catalog.py`) + Qdrant-коллекция `tool_catalog` — почти готовая схема каталога поставщиков, с уже существующим `metadata_` JSON-полем для provenance без миграции.
- `WorkLearning`→`MemoryFact` (`backend/app/domain/work_learning.py`) и `RecipeSkill` (`backend/app/ai/recipes.py`, draft→active→retired lifecycle) — уже работающий self-learning контур, просто ещё не про веб-источники.

Вывод: план — это в первую очередь **сборка и точечное расширение** существующих механизмов, а не новая архитектура с нуля. Это резко снижает риск и объём работ по сравнению с первым черновиком.

Также учтено прямое требование пользователя: (1) любая ошибка/нестыковка, найденная по ходу выполнения плана — фиксируется и исправляется тут же, не откладывается молча; (2) каждый значимый этап завершается пересборкой затронутых сервисов живого docker-стека и прогоном тестов на пересобранном стеке, а не только «тесты прошли на старом образе».

## Что делать в первую очередь при переходе к реализации

Первым шагом (до начала Фазы 0) — сохранить этот план как файл в репозитории, например `AGENT_AUTONOMY_ROADMAP.md` в корне (по аналогии с существующими `CAD_REDRAW_ACCURACY_TODO.md`, `AGENT_SYSTEM_DEVELOPMENT_PLAN.md`, `DXF_CAD_DEVELOPMENT_PLAN.md`), с теми же чекбоксами `- [ ]`/`- [x]`. Именно этот файл в репозитории — рабочий трекер, который редактируется по ходу выполнения (коммитится вместе с изменениями); файл в `/root/.claude/plans/` — только исходный черновик для approval.

---

## Правило 0 — действует на весь roadmap

- [ ] Любая ошибка, нестыковка, устаревшее допущение или расхождение с реальным кодом, обнаруженные при выполнении ЛЮБОГО пункта — не игнорируются. Порядок: (1) фиксируется как новый TODO в подразделе `### Находки по ходу` текущей фазы с кратким описанием; (2) исправляется в рамках той же фазы (если это меняет объём последующих фаз — соответствующие пункты корректируются по факту, не переписываются задним числом); (3) отмечается выполненным только после подтверждения пересборкой+тестами.
- [ ] Каждая фаза начинается с пустого подраздела `### Находки по ходу` — остаётся в файле даже если пустой (аудируемость).

### Чек-лист «пересборка + тесты» (после каждой значимой фазы/подфазы)

- [ ] Точечная пересборка изменённых сервисов: `docker compose -f infra/docker-compose.yml -f infra/docker-compose.dev.yml build <service>` для каждого реально изменённого (`backend`, `celery-worker`, `celery-beat`, `skill-runner`, `frontend`) → `up -d <service>`.
- [ ] `make test` (pytest, не-live) — обязательно.
- [ ] `make test-live` — если фаза трогает живые LLM-пути (планировщик, verifier, capability_builder, recipes/connector).
- [ ] `make regression` — если фаза трогает extraction/pipeline/manifest-контракты.
- [ ] Изменения в `app/tasks/*` → дополнительно `make logs-celery`, убедиться, что beat поднял новые задачи без traceback.
- [ ] Красный результат любого шага — сам становится находкой по Правилу 0, не «пока сойдёт».
- [ ] Полная `make rebuild` — обязательна перед Фазой 4 (сквозной пилот) и перед закрытием всего roadmap.

### Чек-лист «проверка на реальных данных» (где применимо)

- [ ] Конкретный сценарий (curl/скрипт/Playwright, `example-invoices/` или реальные сайты поставщиков) с явным ожидаемым результатом — не абстрактное «проверили».

### Зависимости между фазами

Фаза 0 блокирует Фазы 2, 4, 5 (все пишут в `MemoryFact` с новым scope и/или читают feedback-сигнал). Фаза 1 и 3 не зависят друг от друга напрямую, но интеграция «веб-каталог → extraction pipeline» требует готовой Фазы 2. Фаза 6 зависит от Фазы 5 (ревалидация connector-стратегий — часть idle-job). Фаза 7 независима, может идти в любой момент.

---

## Фаза 0 — Память: scope/tenancy MemoryFact + feedback-сигнал для проактивных задач — ЗАКРЫТА (2026-08-20)

### Находки по ходу
- [x] **Тест `tests/scripts/test_project_ifc_views.py` был сломан на `main`** ещё до этого roadmap — импортировал `_depth_visible` и др. из `scripts/project_ifc_views.py`, но Ф5.1 перенесла эту логику в `app/ai/ifc_reader.py`, оставив `scripts/project_ifc_views.py` тонкой CLI-обёрткой. Из-за этого `python3 -m pytest backend/tests` падал на сборе ВСЕГО набора (`Interrupted: 1 error during collection`) — `make test` был полностью нерабочим на чистом `main`. Починено: импорт в тесте переключён на `app.ai.ifc_reader` (перепроверено на стеше `main` — падало и там, не моя регрессия).
- [x] **`UserInfo` (`app/auth/models.py`) не нёс `department_id`** — ни JWT-путь (`_verify_token`/`_verify_local_session`), ни delegation-путь (`get_effective_user` в `acting.py`) его не резолвили, хотя `User.department_id` в БД уже есть. Без этого department-scope в MemoryFact не из чего было бы наполнять. Починено по образцу уже существующего `_db_section_access_for_sub` (тот же redis-кэш на 45с, тот же fail-open): новая `_db_department_id_for_sub` в `jwt.py`, подключена в оба места верификации токена + в `acting.py`; инвалидация добавлена в `invalidate_active_cache` (уже вызывается из `admin.py` при смене `department_id`, отдельного вызова добавлять не пришлось).
- [x] **Мои же изменения ломали порядок моков в `test_proactive.py`** — throttle-check и per-item snooze-check добавляют новые `db.execute()` вызовы перед уже существующими в `_check_due_dates`/`_dispatch_due_reminders`/`_check_stale_approvals`. Найдено и исправлено сразу (обновлён `_mock_db_ctx` + все затронутые тесты), прежде чем стало «зелёным по недосмотру».
- [x] **`is_snoozed` изначально не был scoped по `beat_task_name`** — снус на «счёт скоро к оплате» тихо заглушил бы не связанный нудж «подтверждение просрочено» по тому же invoice_id. Замечено и исправлено при проектировании, до того как это стало багом в проде.
- [x] `tests/tools/*.py` (6 файлов) и `tests/domain/test_aiagent_capability_contract.py` падают при запуске **внутри backend-контейнера** (`FileNotFoundError: /tools/cad-dataset/...`, `/infra/...`) — НЕ регрессия и не баг: эти тесты вычисляют путь к репо через `Path(__file__).resolve().parents[3]`, что корректно только при полном чекауте (`make test` на хосте); slim-образ backend содержит только поддерево `backend/`. Аналогично `test_skill_runner_isolation.py` (ищет `/infra/docker-compose.yml`, `/backend/app/...`). Не чинится — работает как задумано при штатном запуске `make test` с хоста; при контейнерном прогоне просто `--ignore`.
- [x] Полный прогон `backend/tests` внутри пересобранного `backend`-контейнера против реальной БД: 61 упавший тест, все — до́ моих изменений и не по вине диффа. Выборочно проверено 4/61 трассировкой: `test_provider_registry.py`/`test_routing_pipeline.py` падают на `httpx.HTTPStatusError 404` к `http://host-gateway:11434/api/chat` (нет живого Ollama на хосте в этом окружении), `test_skill_runner_isolation.py` — тот же host-path артефакт, что и у `tests/tools/`. Ни один из 61 не импортирует ни один из изменённых в Фазе 0 файлов (`memory.py`, `models.py`, `jwt.py`, `acting.py`, `proactive.py`, `services/notifications.py`, `api/notifications.py`) — не регрессия.
- [x] Хостовый прогон тестов упирается в порт-коллизию: на `localhost:5432` в этой sandbox-среде слушает Postgres **другого**, не относящегося к проекту docker-проекта (`china-key-learning-postgres-1`, публикует порт на хост), а собственный `infra-postgres-1` порт наружу не публикует (правильно, порт закрыт). Эвристика `backend/tests/conftest.py::_resolve_db_url` находит чужой слушающий порт по одному TCP-коннекту и не проверяет, что это действительно наша БД — падает на `InvalidPasswordError` вместо грациозного отката на testcontainers. Это artefact данной shared-хост среды, не баг репозитория для типичного (одно-проектного) хоста — не чинится; тесты в этой сессии гонялись `docker compose exec backend pytest` (сеть `infra_app`, `POSTGRES_HOST=postgres`), это и есть верный обход именно для этой среды.

### 0.A Scope/tenancy — сделано

- [x] Задокументирован baseline `MemoryFact.scope` (`session`/`project`/`global`/`owner:{sub}`), писатели — `work_learning.py`, `graph_analytics.py`, `api/memory.py`.
- [x] Добавлен префикс `department:{department_id}` (без композиции с другими scope). Источник — `User.department_id` (уже был в БД), теперь резолвится в `UserInfo.department_id` (см. находки выше).
- [x] Миграция не понадобилась (scope — свободная строка); индекс `(scope, kind)` не добавлялся — YAGNI, добавить по факту нагрузки.
- [x] `backend/app/api/memory.py` — правила видимости вынесены в `_resolve_visible_scopes(payload, *, department_id)`; `_search_memory_facts` и `search_memory` обновлены; ветка `department:{id}` добавляется аддитивно на каждой из 4 существующих веток (session/owner/explicit/default) — при `department_id=None` поведение бит-в-бит совпадает со старым (проверено юнит-тестами на скомпилированный SQL).
- [x] Запись `scope=department:{id}` — явный opt-in через уже существующий свободный параметр `scope` в `POST /chat-turn`/`POST /pin` (новый код на write-стороне не понадобился — API и так принимал произвольную строку).
- [x] Прочие читатели `MemoryFact.scope` (`work_learning.py`, `recipes.py` — точечные `owner:{sub}`/`project` лукапы, не general-purpose видимость) — не трогаются, новый префикс их не задевает.
- [x] Тесты: `backend/tests/test_memory_scope_visibility.py` (9 тестов, компиляция SQL-предиката) — зелёные и на хосте, и в контейнере против реальной БД.

### 0.B Feedback-loop для проактивных задач — сделано

- [x] Новая таблица `proactive_task_feedback` (`backend/app/db/models.py`, миграция `20260820_0001_proactive_task_feedback.py`) + `notifications.source_task` (nullable, кто из проактивных задач создал уведомление — только `check_due_dates`/`dispatch_due_reminders`/`check_stale_approvals`; broadcast-задачи `alert_critical_anomalies`/`morning_briefing`/`alert_duplicate_invoices` НЕ в скоупе калибровки — у них нет одного адресата, задокументировано в коде).
- [x] `POST /api/notifications/{id}/feedback` (`accept|dismiss|snooze`, `backend/app/api/notifications.py`) — всегда помечает `is_read=True`; калибрует только если `source_task` не пуст (`calibrated: bool` в ответе).
- [x] `backend/app/domain/proactive_feedback.py`: `get_proactive_task_acceptance_rate`, `should_throttle_proactive_task` (self-throttle по тому же redis-JSON паттерну, что и `GraphAnalyticsSettings`), `record_proactive_feedback`, `is_snoozed` (scoped по `beat_task_name`, не только по entity — снус на один нудж не глушит другой про тот же объект).
- [x] `_check_due_dates`/`_dispatch_due_reminders`/`_check_stale_approvals` подключены: throttle-check в начале, snooze-check перед каждым уведомлением (включая Telegram-дубль), `source_task` проставляется.
- [x] Frontend-кнопка accept/dismiss/snooze в notification UI — **не сделано в этой итерации**, зафиксировано как отдельный открытый TODO (API + backend-тесты полные, UI — следующий проход).
- [x] Тесты: `test_proactive_feedback.py` (12, unit) + `test_notifications.py` (5 новых, требуют live Postgres — прошли в контейнере) + обновлённые `test_proactive.py` (10, под новые вызовы).

### 0.C Пересборка и проверка — сделано
- [x] `docker compose ... build backend celery-worker celery-beat` — собралось (кэш слоёв, менялся только код).
- [x] `docker compose ... run --rm backend alembic upgrade head` — `20260819_0008 → 20260820_0001` применена на живой БД без ошибок; данные (5 users, 153 documents) целы после recreate postgres/redis (config-hash mismatch из-за `--env-file`, не потеря volume).
- [x] `docker compose ... up -d backend celery-worker celery-beat` — все `healthy`; `celery-beat` логи: `check_due_dates`/`dispatch_due_reminders`/`check_stale_approvals` отработали по расписанию без traceback (throttle/snooze-код прошёл вживую на реальных Postgres+Redis).
- [x] `make test` не работал на хосте (см. находки) → прогнано внутри пересобранного `backend`-контейнера (`docker compose exec backend pytest`, реальная `aiworkspace_test` БД по сети `infra_app`): 84/84 на изменённых/новых файлах; полный набор (минус host-path-зависимые `tests/tools/`) — 3186 passed, 61 pre-existing failed (см. находки), 39 skipped.
- [x] **Frontend-кнопка feedback — реализована при закрытии roadmap (после Ф7).** `frontend/components/ui/notification-bell.tsx` — три кнопки (✓ Полезно / ⏰ Позже / ✕ Не нужно) на каждом уведомлении, зовут уже существующий с Ф0 `POST /api/notifications/{id}/feedback`. Показаны на ВСЕХ уведомлениях, не только проактивных — бэкенд и так тихо no-op'ается (`calibrated: false`) для нет-`source_task` уведомлений, а список-эндпоинт вообще не отдаёт `source_task` на фронт, так что client-side фильтрация потребовала бы нового API-поля ради нулевой пользы. `tsc --noEmit` — 0 ошибок, `eslint` — чисто, полный `next build` — `✓ Compiled successfully`, линт и тайпчек прошли без ошибок по всему проекту.

---

## Фаза 1 — Exploratory-режим WorkOrder: точечные расширения — ЗАКРЫТА (2026-08-20)

### Находки по ходу
- [x] **`fail_attempt` никогда не сбрасывал `step.lease_owner`/`lease_expires_at`** — только `reclaim_expired_leases` (другой путь в `retry_wait`, после исчезновения воркера) это делал. Из-за этого шаг, упавший по обычному пути (`execute_claimed_step`'s exception-хендлеры), становился claimable только после истечения СТАРОЙ 120-секундной аренды с первого claim'а — независимо от того, каким коротким был вычисленный exponential backoff (мог быть 5с). Ретраи были незаметно rate-limited до ~120с минимум. Найдено тестом на checkpoint-ретрай (`claim_ready_step` не находил "готовую" retry_wait-запись), исправлено — `fail_attempt` теперь чистит lease в обеих ветках (retry и terminal), плюс отдельный регрессионный тест.
- [x] В проде: в новом блоке `except PartialProgressError` забыт обязательный kwarg `actor=` у `fail_attempt` — поймано интеграционным тестом (`execute_claimed_step` end-to-end), не догадка/ревью, реальный `TypeError` при первом прогоне против живой БД.
- [x] Мои же тесты на `coverage_report`-критерий изначально ожидали blocker `verification_failed`, но при паре критериев (детерминированный `coverage_report` + семантический `honest_not_found`) существующий (неизменяемый) приоритет в `verify_nonempty_result` всегда возвращает `independent_verification_required`, пока семантический критерий не резолвится — это корректное поведение FSM, не баг; тесты переписаны на проверку статуса конкретного `WorkAcceptanceCriterion`, а не агрегированного blocker-кода.
- [x] Тест `max_tool_calls`-бюджета изначально подставлял `attempt_id=uuid.uuid4()` в `WorkToolCall` — поймано FK-ошибкой на реальном Postgres (`work_tool_calls_attempt_id_fkey`), локальный SQLite/мок этого не поймал бы. Переписан на 3 реальных `WorkStepAttempt` через `claim_ready_step`.
- [x] `cost_usd` (как и `checkpoint` до Фазы 1) был мёртвым полем — никто и никогда его не писал. Раз строится бюджет `max_cost_usd` поверх него — `complete_attempt` теперь тоже заполняет `attempt.cost_usd` из `output.cost_usd` (симметрично уже существовавшему `tokens_used`), иначе бюджет был бы недостижим на практике.

### 1.A Флаг режима — сделано

- [x] `is_exploratory(order)` — читает `order.constraints["mode"] == "exploratory"`, JSON-поле, миграция не понадобилась.
- [x] **Decompose-механизм переиспользован как есть** (`PlannedStep.kind="decompose"`, `_split_child_budgets`) — новый «горизонт-цикл» не писался, он не нужен: существующий incremental replanning (`completed_context`/`failure_context` в `generate_capability_plan`) уже даёт именно это.
- [x] Промпт планировщика (`backend/app/domain/work_planning.py`) — условная добавка к system-промпту при `is_exploratory(order)`: горизонт 1-3 шага вместо полного DAG, предпочтение decompose для независимых юнитов, обязательный контракт финального вывода `{text, coverage:{covered,partial,not_found}}`.

### 1.B Checkpoint — сделано (в объёме retry-возобновления, не потокового progress)

- [x] Архитектурное решение, зафиксированное явно: mid-execution потоковый progress НЕ строился — философия существующей архитектуры («маленькие шаги + replanning», явная фраза в промпте планировщика «smallest executable DAG») делает это ненужным для MVP. Вместо этого — **checkpoint на failure-retry пути**: новое исключение `PartialProgressError` (`backend/app/tasks/work_orders.py`) — капабилити, упавшая с реальным прогрессом, сообщает `checkpoint` в теле ответа (`{"error":..., "checkpoint": {...}}`, конвенция, не обязательный контракт); `_execute_capability` парсит и поднимает `PartialProgressError`; `fail_attempt` сохраняет `checkpoint` на упавшем attempt; следующая попытка (`execute_claimed_step`) подмешивает его в `input_data["_resume_checkpoint"]`, если план сам не задал ключ явно.
- [x] Тесты: `_execute_capability` поднимает `PartialProgressError` только когда `checkpoint` реально есть в теле (4xx/5xx/200-с-ошибкой); `fail_attempt` персистит/не персистит корректно; полный e2e (attempt 1 падает с checkpoint → attempt 2 получает `_resume_checkpoint`) — все на реальном Postgres.

### 1.C Бюджеты — сделано (расширен существующий `enforce_budgets`, не написан параллельный механизм)

- [x] `enforce_budgets` (Б15) расширен тремя новыми измерениями поверх уже бывшего `token_budget`: `max_cost_usd` (сумма `WorkStepAttempt.cost_usd`), `max_wall_clock_seconds` (от `order.started_at`), `max_tool_calls` (счётчик строк `WorkToolCall` по `work_order_id`, независимо от статуса). Проверяются в фиксированном порядке — первый превышенный бюджет определяет причину блокировки.
- [x] Целенаправленного `max_web_requests` (именно веб-запросы отдельно от прочих tool calls) не строилось — честно отложено до Фазы 2, где появится реальная веб-капабилити (`web_discover`) для которой это имело бы смысл; `max_tool_calls` — рабочий общий механизм уже сейчас, не заглушка.
- [x] Тесты: `max_cost_usd`, `max_wall_clock_seconds` (блокирует/не блокирует), `max_tool_calls`, приоритет причины при одновременном превышении нескольких бюджетов — все на реальном Postgres.

### 1.D Honest-coverage — сделано (без изменения FSM/verdict-путей)

- [x] `exploratory_acceptance_criteria()` — пара критериев для `create_work_order(acceptance_criteria=...)`: `coverage_report` (детерминированный, новая ветка `predicate_type == "coverage_report"` в `verify_nonempty_result` — проверяет ТОЛЬКО форму `{covered:[...], partial:[...], not_found:[...]}` и что не все три списка пусты, не исчерпанность) + `honest_not_found` (семантический, независимый verifier — **нового кода в `verify_semantic_criteria` не понадобилось**: существующий verifier уже судит по `predicate.description`, туда просто вписано требование честности).
- [x] Глобальный gate `has_result` (непустой `text`) в `verify_nonempty_result` по-прежнему применяется — у exploratory-финального-шага обязателен человекочитаемый `text` рядом со структурным `coverage`, отдельный тест на этот случай.
- [x] Тесты: well-formed report проходит `coverage_report`; пустой/malformed — падает; отсутствие `text` — блокирует несмотря на хороший `coverage`; промпт-подсказка планировщика подтверждена мок-тестом на `generate_json`.

### 1.E Пересборка и проверка — сделано
- [x] Пересобраны и перезапущены `backend`/`celery-worker`/`celery-beat` — миграция не требовалась (все изменения — существующие JSON/колонки). `celery-beat` прогнал `work.dispatch_ready` (гоняет `enforce_budgets` вживую) без ошибок на реальном трафике (бэкенд обслуживал реальные запросы пользователя параллельно).
- [x] 56 тестов по work-order домену (exploratory + checkpoint + lease/budgets + verifier + replanning + decompose + computer-use + gap-detection) — зелёные на реальном Postgres в контейнере.
- [x] Полный non-live прогон (3211 passed, 61 pre-existing failed — **идентичный список** тем же 61, что и до Фазы 1, побайтово сверено diff'ом) — ноль регрессий.

---

## Фаза 2 — Веб-доступ через ComputerUseGrant + notify-before-scope — ЗАКРЫТА (2026-08-20)

### Находки по ходу
- [x] **Критический баг: `execute_computer_action` использовал `get_current_user`, а не `get_effective_user`.** Когда WorkStep вызывает `computer_use` через штатный путь (`_execute_capability` → `dispatch_capability` → `/api/computer-use/execute`), запрос несёт `X-API-Key` агента + `X-Acting-User: <human sub>`; `get_current_user` резолвит такой запрос в `sub="agent-service"` (см. `_verify_api_key`), НЕ учитывая `X-Acting-User` — обе проверки (`order.owner_key != user.sub` и `grant.granted_to == user.sub`) проваливались с 404 для ЛЮБОГО WorkOrder, принадлежащего реальному человеку. Весь `computer_use` был неработоспособен при вызове из durable runtime (работал только при прямом вызове человеком из браузера). Исправлено: заменена зависимость на `get_effective_user` (тот же паттерн, что уже используется в `memory.py`). Живой тест `test_agent_service_with_acting_header_resolves_to_that_human` подтверждает резолюцию.
- [x] **`get_effective_user` не имел вообще ни одного теста**, хотя на нём держится de-escalation агент→человек в нескольких местах кодовой базы. Добавлен `test_auth_acting.py` (6 тестов, включая деактивированного/несуществующего acting-пользователя — fail-closed на сервисный аккаунт).
- [x] **Generic Approval-retry не подходит для `computer_use`**: одобрение digest'а (`X-Agent-Approval: granted`) не создаёт `ComputerUseGrant` — это отдельная авторизационная сущность, создаваемая только вручную менеджером (`POST /work-orders/{id}/computer-grants`). Одобрение сгенерированного `Approval` для `computer_use`-действия молча ничего не решает. Это и есть настоящее содержание «notify-before-scope, не approval» из плана — реализовано как отдельное уведомление (`_notify_computer_use_needs_grant`), не расширение approval-механизма.
- [x] Мой собственный баг в первой версии notify-фикса: вызвал `_notify_computer_use_needs_grant` **после** `await db.commit()` — `create_notification` только делает `add()+flush()`, не коммитит сама; уведомление создавалось в новой имплицитной транзакции, которая откатывалась при закрытии сессии. Пойман тестом (`notif is not None` падал), перенёс вызов до commit.
- [x] Ни `execute_web_search`, ни `browser_fetch` не были доступны как WorkStep-capability для «поиска» (только точечный `browser_fetch` по known URL) — `web_discover` заполняет этот пробел.
- [x] Планировщик берёт допустимые actions капабилити из pipe-separated строки в `parameters.properties.action.description` капабилити в `capabilities.yml` (не из dispatch-таблицы) — без добавления `web_discover` туда `validate_capability_plan` отклонял бы любой сгенерированный шаг с этим action как «unknown action».

### 2.A Веб-доступ через существующие механизмы — сделано

- [x] `execute_web_research`/`POST /research` уже делает почти всё нужное (multi-query search + параллельный fetch + per-URL diagnostics, никогда не бросает) — **переиспользованы** его составляющие функции (`execute_web_search`, `fetch_page`), не сам endpoint (он не проверяет allowlist хостов вообще — для exploratory-режима с неизвестными заранее хостами это неприемлемо без grant-скоупинга).
- [x] Новый endpoint `POST /api/computer-use/web-discover` (`backend/app/api/computer_use.py`) + action `web_discover` капабилити `computer_use` (`_DISPATCH`, `capabilities.yml`) — search по нескольким query-углам, fetch только host-allowed URL, 1 unit `max_actions` за каждый реальный fetch, полный audit trail (`ComputerUseAction` на каждый fetch). Никогда не бросает из-за одного плохого query/URL — `search_diagnostics`/`skipped` вместо исключения.
- [x] `_host_allowed` расширен явным wildcard-сентинелом `"*"` — exploratory-задача не может заранее перечислить хосты, которые обнаружит поиском; пустой список по-прежнему означает «ничего», wildcard — осознанный акт менеджера при выдаче granta (та же ролевая защита, что и у именованных хостов).
- [x] Никакого обхода robots.txt/логинов/капч — не реализовывалось и не рассматривалось; `fetch_page`/`execute_web_search` — уже существующие безопасные обёртки, новый код их не трогает.
- [x] Пересмотр approval-гранулярности **не понадобился** — `gate_actions: [desktop_click, desktop_type, file_write, shell]` в `capabilities.yml` уже НЕ включает `browser_fetch`/`desktop_snapshot`/`file_read` (не `["*"]`, как предполагал первый черновик плана); `web_discover` добавлен туда же, тоже вне gate_actions.
- [x] Тесты: wildcard host-matching (5), web_discover (грант обязателен / host-фильтрация / wildcard / budget exhaustion / устойчивость к ошибке поиска) — все на реальном Postgres.

### 2.B Notify-before-scope — сделано (иначе, чем в исходном плане, но по существу)

- [x] Исходный план предполагал pre-emptive оценку стоимости на входе в `running`. По факту оказалось точнее и полезнее **реактивное** уведомление: exploratory-план по природе не знает заранее, понадобится ли `computer_use` (small-horizon planning из Ф1.A) — оценивать это до первого реального шага означало бы гадать. Уведомление шлётся в момент, когда шаг РЕАЛЬНО упирается в отсутствие granta (`ApprovalRequiredError` с `capability=="computer_use"`), с чёткой инструкцией «создать grant может только менеджер», отдельно от текста про «approve».
- [x] Явно отличается от approval: **не блокирует** отдельным статусом (шаг всё равно уходит в `waiting_approval`, existing FSM, дублирования не потребовалось), но текст уведомления однозначно говорит, что делать нужно НЕ то же самое, что «одобрить».
- [x] `Notification.source_task="workorder.needs_computer_use_grant"` — задел на калибровку через `ProactiveTaskFeedback` (Фаза 0.B) оставлен, но accept/dismiss UI для этого конкретного уведомления не подключался (это одноразовое событийное уведомление, не периодическая проактивная задача — калибровка для него менее естественна, чем для beat-заданий).
- [x] Тесты: уведомление создаётся с `entity_id`/`user_sub`/текстом для `computer_use`-капабилити; **регрессия проверена явно** — для любой другой capability (`invoices.approve` и т.п.) уведомление НЕ создаётся, старое поведение (только digest-Approval) не тронуто.

### 2.C Пересборка и проверка — сделано
- [x] Пересобраны и перезапущены `backend`/`celery-worker`/`celery-beat`; `aiagent/` смонтирована в контейнер read-only bind-mount'ом — правка `capabilities.yml` подхватывается без пересборки, но сервисы всё равно перезапущены для консистентности с код-изменениями.
- [x] 19+48 тестов (web_discover/computer_use/auth_acting + регрессия work-order/exploratory/checkpoint/lease) зелёные на реальном Postgres в контейнере.
- [x] Полный non-live прогон на пересобранном стеке: 3229 passed (+18 к Фазе 1), 61 pre-existing failed — **идентичный список**, побайтово сверено diff'ом с прогоном Фазы 1. Ноль регрессий.

---

## Фаза 3 — Extraction/normalization пайплайн для каталогов поставщиков — ЗАКРЫТА (2026-08-20)

### Находки по ходу
- [x] **Огромная переоценка объёма работ, в хорошую сторону**: `backend/app/tasks/drawing_analysis.py::_ingest_catalog_async`/`ingest_supplier_catalog` (Celery-таска) — уже ПОЛНОСТЬЮ рабочий пайплайн файл→строки→`ToolCatalogEntry`→embed→Qdrant→граф (`ingest_tool_catalog_graph`), задействован через существующие `upload_catalog`/`refresh_catalog` эндпоинты. `backend/app/domain/pipeline.py` (11 строк, только имена стадий) НЕ является реальной оркестрацией, как предполагал первый черновик плана — реальный, пригодный к переиспользованию пайплайн оказался в `drawing_analysis.py`, не в invoice-пайплайне `document_processing.py`. Экономия: Qdrant/граф-интеграция (3.D) не потребовала НИ строки нового кода — только рефакторинг существующего цикла в переиспользуемый helper.
- [x] `_parse_catalog_file` (диспетчер по расширению) поддерживает только `.xlsx/.xls/.csv/.json/.pdf` (через `pdfplumber.extract_tables()` — только PDF с нативными таблицами, не сканы/free-text) — **HTML вообще не поддерживался**. Это прямая брешь для веб-контента: `fetch_page` (Ф2 `web_discover`) возвращает уже ИЗВЛЕЧЁННЫЙ текст (HTML→текст и OCR PDF→текст уже сделаны внутри), не сырые байты файла — значит для веб-источников нужен НЕ файл-парсер, а text→rows LLM-экстрактор. Построен `_parse_catalog_text_via_llm` как отдельный, параллельный путь (не попытка втиснуть текст в `_parse_catalog_file`).
- [x] `tool_catalog` вообще не была зарегистрирована как capability (ни в `_DISPATCH`, ни в `capabilities.yml`) — заведена с нуля.
- [x] `"approve"` уже есть в `RISKY_CAPABILITY_ACTIONS` (`policy_engine.py`) — approve-действие автоматически требует нахождения в `gate_actions`, иначе `_validate_capability_contract` fail-closed с 503 `gate_missing`. Ничего изобретать не пришлось — просто назвал action `approve` и добавил в `gate_actions`, существующий защитный механизм сработал как задумано.
- [x] **Мой собственный баг счётчиков**: в исходной (и унаследованной от pre-Ф3 кода) структуре цикла `created += 1` стояло ПОСЛЕ вызова embed/Qdrant — если тот падал, строка попадала в `except` и считалась `skipped`, хотя `db.add(entry)+flush()` уже реально положил запись в сессию, которую внешний `commit()` всё равно сохранял. Запись существовала в БД, но отчитывалась как «пропущенная» — прямое нарушение принципа честного purchase Ф1.D. Исправлено: `created += 1` сразу после flush, embed/graph обёрнуты в свой try/except, который пишет в `errors`, но не трогает счётчики.
- [x] **Архитектурный баг в новом коде**: `ingest_web_catalog_source` изначально открывал собственную сессию через `_get_session_factory()` (паттерн, правильный для Celery-задач в этом файле), хотя вызывается СИНХРОННО из HTTP-хендлера, у которого уже есть request-scoped `db` через `Depends(get_db)`. В проде «работало» (обе сессии смотрят в одну БД), но тест сразу поймал разрыв (созданный в тестовой транзакции поставщик был не виден). Исправлено: функция теперь принимает `db` от вызывающего, эндпоинт передаёт свою сессию.

### 3.A Провенанс без миграции — сделано

- [x] `ToolCatalogEntry.metadata_` (уже существующее JSON-поле) — provenance (`source_url`, `fetched_at`, `title`, `discovery_method` неявно через сам факт наличия остальных полей) для web-sourced записей; ручной upload/refresh не получает `metadata_` вообще (как и раньше — обратная совместимость подтверждена тестом).
- [x] `review_status` (`ingested` | `needs_review`) — та же `metadata_`, без миграции.

### 3.B Draft-first + конфликты — сделано

- [x] Web-sourced записи получают `metadata_.review_status="ingested"`. При конфликте (совпадающий `supplier_id`+`part_number`, но существенно другая цена/название) — существующая запись НЕ трогается, новая создаётся с `review_status="needs_review"` + `AnomalyCard` (`anomaly_type=duplicate`, `entity_type="tool_catalog_entry"`), связывающая обе записи. Повторное обнаружение НЕизменившегося листинга конфликтом не считается (проверено тестом) — иначе re-crawl того же поставщика плодил бы аномалии на пустом месте.
- [x] `POST /api/tool-catalog/entries/{id}/approve` — снимает `review_status`, approval-gated (`gate_actions`), идемпотентен для записей без review_status (ручные записи).

### 3.C Хранение и text-based extraction — сделано (иначе, чем в плане: не файловый пайплайн, а text-пайплайн)

- [x] Сырой текст веб-источника — через `backend/app/storage.py` (`upload_file`), путь `tool-catalogs/{supplier_id}/web/{sha256(url)[:16]}.txt`, best-effort (ошибка хранения не блокирует извлечение). `domain/storage.py` не использовался (как и требовал план).
- [x] Парсер-реестр `parsers/registry.py` НЕ переиспользовался — он для файлов с байтами на входе, а не для уже-извлечённого текста от `fetch_page`. Вместо него — новый `_parse_catalog_text_via_llm` (JSON-schema-constrained LLM-вызов, тот же паттерн `generate_json`/`get_reasoning_model`, что уже в `work_planning.py`), никогда не бросает — malformed/недоступная модель → пустой список строк.
- [x] `pipeline.py` не трогался и не форкался — подтверждено находкой выше, что он не является местом интеграции.

### 3.D Qdrant `tool_catalog` + граф поставщиков — сделано (переиспользование, не новый код)

- [x] Существующая коллекция `tool_catalog` (`upsert_tool_catalog_entry`) и `ingest_tool_catalog_graph` — используются БЕЗ ИЗМЕНЕНИЙ через общий `_create_catalog_entries_from_rows`, вынесенный из `_ingest_catalog_async` рефакторингом (единый код для файлового и веб-пути — эмбеддинг/граф/Qdrant не дублируются).
- [x] Отдельная `ingest_supplier_catalog_graph()` не понадобилась — существующая `ingest_tool_catalog_graph(entry_id, db)` уже per-entry и полностью подходит.
- [x] Тесты (на реальном Postgres+Qdrant+Ollama в контейнере): entries создаются с provenance, конфликт → AnomalyCard + существующая запись нетронута, embed/Qdrant/граф проходят по-настоящему (не мокались, как и остальные тесты `test_tool_catalog.py` в этом проекте).

### 3.E Пересборка и проверка — сделано
- [x] Пересобраны и перезапущены `backend`/`celery-worker`/`celery-beat`, миграция не требовалась.
- [x] 33 теста (web_catalog_ingestion + существующий test_tool_catalog.py + capability_catalog_consistency) зелёные на реальных Postgres/Qdrant/Ollama.
- [x] Полный non-live прогон: 3244 passed (+15 к Фазе 2), 61 pre-existing failed — **идентичный список**, побайтово сверено diff'ом с прогоном Фазы 2. Ноль регрессий.

---

## Фаза 4 — Сквозной пилот: «найди и структурируй каталоги поставщиков» end-to-end

### Находки по ходу
- [x] **`FASTAPI_URL` нигде не был задан для celery-сервисов.** `BuiltinAgentConfig.backend_url` дефолтится в `http://localhost:8000` — корректно только для кода, исполняющегося ВНУТРИ backend-контейнера (например, `execute_work_order_now`, вызванный синхронно из FastAPI-хендлера). Любой WorkStep, диспетчеризованный через Celery (`work.execute_step`, реально исполняется в `celery-worker`/`-gpu`/`-lora`, НЕ в backend), вызывает `_execute_capability` → httpx POST на этот URL — без переменной окружения пытался достучаться до `localhost:8000` ВНУТРИ worker-контейнера, где никакого FastAPI-сервера нет. **Это означает, что вся durable-раннер capability-система для ЛЮБОЙ capability (не только моих из Ф2-Ф3) была сломана при диспетчеризации через Celery с момента, как эта архитектура появилась** — работала только синхронная `execute_work_order_now`. Исправлено: `FASTAPI_URL: http://backend:8000` в общем YAML-anchor `x-backend-env` (`infra/docker-compose.yml`) — применяется ко всем сервисам сразу, включая backend (безвредно для него).
- [x] **`set_acting_user` нигде не вызывался в durable-раннере.** Каждый capability-вызов из WorkStep аутентифицировался как голый `agent-service` (fail-closed дефолт из `app/ai/actor_context.py`), без acting-user контекста. Безвредно для capability без owner-проверки, но ЛЮБОЙ эндпоинт, использующий `get_effective_user` для скоупинга по владельцу WorkOrder (`computer_use.execute`/`web_discover`, исправленные в Ф2.A именно под этот кейс «агент действует от имени человека»), получал 404 на ЛЮБОМ реальном WorkOrder (кроме буквально принадлежащих "agent-service"). Мой же Ф2.A фикс был функционально бесполезен в durable runtime без этого. Исправлено: `set_acting_user(order.owner_key)` перед `_execute_step_kind` в `execute_claimed_step`, сброс в `finally` (worker-процесс может продолжить другими задачами).
- [x] **`success_predicate` от reasoning-модели иногда приходит строкой (прозой), а не dict'ом** — базовый системный промпт называет поле в схеме, но не даёт примера формы. Модель написала "Supplier created successfully with a new supplier_id" вместо `{"type": ...}` → `PlannedWork.model_validate` падает с `ValidationError` → **весь многошаговый/decompose план выбрасывается**, durable planner откатывается на одношаговый `agent_turn`-fallback, вся exploratory-механика не задействуется. Обнаружено на первом же живом вызове планировщика. Проверено: `WorkStep.success_predicate` нигде не читается в runtime (чисто для аудита/отображения) — значит можно безопасно **coerce**, не отклонять план. Исправлено: `field_validator(mode="before")` на `PlannedStep.success_predicate` — строка превращается в `{"type": "custom", "description": <строка>}`.
- [x] **`promote_waiting_parents` строил только текстовую агрегацию**, не структурированный `output.coverage` — `exploratory_acceptance_criteria()`'s `coverage_report`-предикат (Ф1.D) требует `{covered, partial, not_found}`, но при decompose-fan-out (паттерн, к которому явно подталкивает промпт планировщика из Ф1.A) никакой другой шаг такую структуру не производит. Без фикса honest-coverage критерий был НЕДОСТИЖИМ для decompose-паттерна вообще. Исправлено: `promote_waiting_parents` строит `coverage` из тех же per-child данных, что уже использует текстовая сводка (не может разойтись с тем, что видит человек в `result_summary`), с `_blocker_reason()`-хелпером, который выкапывает содержательную причину из вложенной структуры `blocker` (`fail_attempt`'s `{"code":"step_failed","error":{"code":"no_grant"}}`), а не отдаёт бесполезный общий `"step_failed"`.
- [x] Дочерние WorkOrder при decompose не наследовали `constraints` (в т.ч. `mode=exploratory`) от родителя — не блокирующая находка (дети создаются с одним `agent_turn`-шагом напрямую, не через планировщик), но влияет на реплан при сбое ребёнка. Исправлено — пробрасывается явно.
- [x] **Capability-шаги не получали `work_order_id`, если модель сама его не написала в `input_data`.** `WebDiscoverIn`/подобные схемы требуют `work_order_id` как обязательное поле, но ничто в durable runtime не подставляло его автоматически — реальный план планировщика содержал только `{"queries": [...], "max_sources": N}`, что давало `HTTP 422` на каждом `computer_use.web_discover`/`tool_catalog.*` шаге. Исправлено: `execute_claimed_step` подставляет `input_data["work_order_id"] = str(step.work_order_id)`, если модель его не задала явно (explicit-value-wins).
- [x] **`reclaim_expired_leases` роняла order-level переход в `blocked`/`replanning` только если `order.status == "running"` в момент истечения лизы шага.** Живой пилот: соседний шаг успел зафиксировать успех и `promote_ready_dependents` откатил заказ обратно в `ready` ДО того, как лиза другого (permanently failed, `max_attempts` исчерпан) шага истекла и была подобрана `reclaim_expired_leases`. `order.blocker` выставлялся, а статус — нет: заказ навсегда зависал в `ready` с недостижимым pending-шагом (его зависимость никогда не станет `succeeded`), при этом `dispatch_ready` тикал каждые 5с без единого найденного действия. Проверено по `WORK_TRANSITIONS`: `"ready"→"replanning"/"blocked"` — легальные переходы, гвард был избыточно узким. Исправлено: гвард заменён на проверку легальности перехода по `WORK_TRANSITIONS.get(order.status, frozenset())` вместо жёсткого сравнения с `"running"` — покрывает все статусы, из которых цель легальна, и по-прежнему безопасно no-op'ается на терминальных (`completed`/`canceled`).
- [x] **Периодический запрос `pending_verification` в `_dispatch_ready_work` не фильтровался по активному плану** — при повторном реплане (несколько `WorkPlan` ревизий на один `WorkOrder`) он на КАЖДОМ тике повторно выбирал уже успешные шаги из УСТАРЕВШИХ (superseded) планов, пока заказ был `running`. `verify_completed_step` вызывал `promote_ready_dependents(plan_id=<старый план>)`, который пересчитывал `unfinished` по шагам СТАРОГО плана (включая его permanently-failed/недостижимые siblings) и откатывал заказ в `ready` — затирая состояние заказа посреди реального выполнения СОВЕРШЕННО ДРУГОГО шага из АКТУАЛЬНОГО плана. Живой пилот: именно так корректно выполнявшийся шаг `synthesize` активного плана-7 получил заказ в статусе `ready` вместо `running` прямо во время своего выполнения. Исправлено: запрос вынесен в доменную функцию `find_active_plan_succeeded_steps` с тем же `WorkPlan.status == "active"` джойном/фильтром, что уже использует `claim_ready_step`.
- [x] **Последствие предыдущей находки: заказ, застрявший в `ready` из-за уже произошедшей порчи (до деплоя фикса), не мог сам восстановиться** — успешный `synthesize`-шаг (единственный и последний в плане) никогда не подхватывался на верификацию, потому что `find_active_plan_succeeded_steps` требует `status == "running"`, а вернуть заказ в `running` было некому: `claim_ready_step` делает это только когда есть ещё `ready`/`retry_wait` шаг для захвата, а такого уже не было. Это отдельный, более общий класс зависания («план дошагал до конца, но заказ не узнал об этом»), не завязанный на конкретный механизм порчи выше. Исправлено двумя слоями: (1) новая доменная функция `unstick_ready_orders_with_stalled_active_plan` — узко скоуплена (заказ `ready`, активный план БЕЗ единого шага в состояниях `ready/retry_wait/running/pending/waiting_approval`, но с хотя бы одним `succeeded`) — переводит такой заказ обратно в `running`, вызывается в `_dispatch_ready_work` перед `find_active_plan_succeeded_steps`; (2) defense-in-depth — `verify_completed_step` сам восстанавливает `ready→running`, если добрался туда напрямую (не только через хаускипинг-проход).
- [x] **`success_predicate` от модели иногда приходит булевым `true`, а не строкой/dict'ом** — тот же корневой источник, что и прозаический строковый случай (Ф4, найдено раньше): базовый промпт называет поле, но не показывает пример формы. `plan.fallback_used` на реплане 7 живого пилота: план с 6 шагами отброшен целиком, планировщик откатился на тривиальный fallback. Раз `WorkStep.success_predicate` нигде не читается рантаймом (только аудит/отображение) — coerce безопасен так же, как для строки. Исправлено: `_coerce_success_predicate` дополнен веткой `isinstance(value, bool)`.
- [x] **`dataflow_resolution_error: 'supplier_id'`** — модель написала `${steps.X.output.supplier_id}` вместо `${steps.X.output.result.id}` (реальная форма — `_execute_capability` заворачивает ответ эндпоинта в `{"result": ..., "result_summary": ..., "executor": "capability"}`). Базовый промпт планировщика УЖЕ показывает этот именно `.result.`-паттерн на собственном примере (`${steps.lookup.output.result.items}`) — это не пробел в промпте, а разовый лапс генерализации модели, а не системная нестыковка. Решение: не патчить `resolve_step_input`/`_path_get` спекулятивным «прощением» путей (риск для механизма, от которого зависит весь dataflow) — задокументировать как наблюдаемое ограничение надёжности локальной модели-планировщика, не как баг кода. Стоил заказу одного replan-цикла (revision 5→6) из бюджета `max_replans=6`.
- [x] Итоговый прогон живого пилота на реальных источниках — см. ниже.

- [x] Собрать компоненты Фаз 1-3 в один реальный exploratory WorkOrder на реальных источниках (без моков). WorkOrder `b32cf39c-4353-452e-ae12-7a9717b7e33b`, владелец — smoke-test аккаунт `claude@test.com`, `ComputerUseGrant` с `allowed_hosts=["*"]`, budgets `max_wall_clock_seconds=1800/max_tool_calls=40/max_replans=6`.
- [x] Прогнать полный цикл: создание → decompose по поставщикам → `web_discover` через `ComputerUseGrant` → `tool_catalog.create_supplier`/`ingest_web_source` → replanning нескольких горизонтов (revision 1→7, все находки выше обнаружены и исправлены именно в процессе этих реплан-циклов) → honest-coverage критерии → терминальный статус. Итог: `blocked`, не `completed` — агент реально нашёл и завёл поставщика Haltec (create_supplier + ingest_web_source), поставщик Betar упал из-за инфраструктурной находки выше (permanently failed после lease_expired), нашёл третьего (YG1), но не уложился в оставшийся бюджет реплана на извлечение его каталога и не оформил честную сводку по нему.
- [x] **Корректировка пользователя (2026-08-20) — `blocked` не «честный успех», это провал цели, который надо чинить/дожимать.** Первая формулировка итога выше рамировала `blocked` как «закономерный, ровно как задумано» исход — пользователь прямо поправил: цель агента — реальное выполнение или исчерпывающая аргументация после genuine попыток, а не быстрое, честно оформленное отступление; «агент должен хотеть выполнить задачу, а не придумать, почему не выполнил». При разборе нашёл прямую причину именно ТАКОГО поведения модели в собственном коде: system-промпт exploratory-режима (`generate_capability_plan`, `backend/app/domain/work_planning.py`) заканчивался фразой **"Reporting a genuine gap in not_found is success, not failure"** — буквально учил модель, что сдаться так же хорошо, как найти. Плюс дефолт `budgets.max_replans=2` (`fail_attempt`/`reclaim_expired_leases`) не различал exploratory/bounded режимы — для exploratory replanning является МЕХАНИЗМОМ ПРОДОЛЖЕНИЯ работы (Ф1.A: «следующий горизонт»), а не только error-recovery, и живой пилот исчерпал большую часть вручную поднятого бюджета (6) на инфраструктурные баги (уже исправленные выше), а не на подлинное исчерпание стратегий. Исправлено тремя фиксами: (1) промпт переписан — убрана фраза «gap = success», добавлено явное требование пробовать другой запрос/источник/инструмент/формулировку перед тем, как признать пункт `not_found`, и подчёркнуто «your goal is to actually complete the objective, not to produce an honest-sounding reason for not completing it»; (2) критерий `honest_not_found` (`exploratory_acceptance_criteria()`) ужесточён — требует НЕСКОЛЬКИХ разных попыток на пункт `not_found`, одна неудачная попытка явно недостаточна, критерий должен провалиться, если очевидная альтернативная стратегия не была опробована; (3) новый `_max_replans_for(order)` — дефолт `30` для exploratory-режима вместо `2` (явный `budgets.max_replans` по-прежнему побеждает в любом режиме), реальным потолком остаются wall-clock/cost/tool-call бюджеты (Ф1.C), а не произвольный счётчик ревизий плана. Тесты: `test_max_replans_for_defaults_higher_for_exploratory_orders_without_an_explicit_budget` и парные (`test_work_order_replanning.py`), плюс `test_exploratory_order_survives_far_more_replans_than_the_bounded_default` — bounded-дефолт блокирует на 2-3 реплане, exploratory тот же сценарий продолжает реплан на 5-м. **Повторный живой пилот на исправленной версии не перезапускался в рамках этой находки** — это открытый TODO (см. ниже), а не подтверждённый результат.
- [x] **Повторный живой пилот (2026-08-20, WorkOrder `5db58ac6-52c3-495e-bcec-b0be42023bd6`) — выполнен, вскрыл ещё 5 реальных находок, все исправлены живьём в процессе прогона (не отложены).** Объектив: те же 4 поставщика (Haltec, Betar, YG1, Sandvik) + честное требование не сдаваться после одной попытки. `budgets` изначально `{max_wall_clock_seconds: 5400, max_tool_calls: 60}` (без явного `max_replans` — эксплуатирует новый дефолт 30). Итог: терминальный `blocked` на ревизии плана 31 (`31 > max_replans=30`) — **честный, не преждевременный**: 2ч15м реального времени, 31 план, 30 провалов шагов (`13 lease_expired`, `16 execution_error` — почти все «модель вернула пустой ответ», `1 dataflow_resolution_error`) — **96.7% провалов были инфраструктурным/model-reliability шумом, не решением агента сдаться**; независимый verifier по ходу пилота многократно фиксировал КОНКРЕТНЫЕ, разные попытки (разные запросы, честно названные нерелевантные результаты — «немецкая ферма» для Haltec, «Toyota Camry» для Betar) — именно то, что требовал ужесточённый `honest_not_found`. Найдено и исправлено живьём:
  - **Провал критерия приёмки вообще не консультировал `max_replans`** — `verify_nonempty_result`'s детерминированная ветка и `record_verifier_verdict`'s семантическая ветка ОБЕ уходили в терминальный `blocked` безусловно, даже когда бюджет реплана оставался нетронутым — весь смысл поднятого до 30 `max_replans` был недостижим для ЭТОГО класса провала (только провалы САМИХ ШАГОВ через `fail_attempt`/`reclaim_expired_leases` когда-либо тратили этот бюджет). Обнаружено на первой же секунде повторного пилота — заказ `blocked` уже на ревизии 1. Исправлено: обе ветки теперь считают `target = "replanning" if plan_revision <= _max_replans_for(order) else "blocked"`, как `fail_attempt`; `"replanning"` добавлен в `WORK_TRANSITIONS["blocked"]` (легальный переход, `"blocked"→"replanning"` нужен для семантической ветки, приходящей туда через промежуточный `independent_verification_required`). Тесты: `test_deterministic_criterion_failure_replans_within_budget`/`_blocks_once_replan_budget_exhausted`, `test_semantic_verifier_rejection_replans_within_budget`/`_blocks_once_replan_budget_exhausted` (`test_work_order_verifier.py`) + 2 теста на decompose-родителя (`test_work_order_decompose.py`, один уже существующий тест обновлён под новое поведение — родитель с неиспользованным бюджетом теперь корректно уходит в `replanning`, не `blocked`).
  - **`WorkAcceptanceCriterion.status` для семантического критерия никогда не сбрасывался между реплан-циклами** — как только `record_verifier_verdict` один раз пометил критерий `"failed"`, `verify_semantic_criteria`'s фильтр `status=="pending"` НАВСЕГДА исключал его из будущих проверок — независимый verifier переставал вызываться вообще, заказ мог реплэниться бесконечно, но никогда не получить настоящий вердикт ни в одну, ни в другую сторону. Обнаружено при разборе, почему заказ завис на `independent_verification_required` дольше ожидаемого. Исправлено: `verify_nonempty_result` сбрасывает `criterion.status`/`verdict` обратно в `"pending"`, когда определяет, что критерий снова требует независимой проверки. Тест: `test_rejected_semantic_criterion_is_reset_to_pending_on_the_next_attempt`.
  - **`dataflow_resolution_error: 'supplier_id'` повторился НЕЗАВИСИМО на ВТОРОМ живом пилоте** — то же самое отсутствие `.result.`-префикса, что в первом пилоте (там задокументировано как «разовый лапс», не патчилось). Повторение на независимом прогоне доказывает: это предсказуемый, а не случайный пробел генерализации модели — оправдывает точечный, низкорисковый серверный fallback вместо документирования как ограничения. Исправлено: `_path_get` при отсутствии сегмента на верхнем уровне пробует на один уровень глубже внутри `.result` (тот самый wrapping, что создаёт `_execute_capability`) — не меняет поведение для уже корректных ссылок.
  - **Реальный Postgres-deadlock между параллельно исполняющимися шагами одного плана** (`asyncpg.exceptions.DeadlockDetectedError`, живьём — 3 из 4 параллельно захваченных `discover_*`-шагов упали с этой ошибкой). Источник: `_heartbeat_step` брал `SELECT ... FOR UPDATE` на общую родительскую `WorkOrder`-строку КАЖДЫЕ 30с ДЛЯ КАЖДОГО параллельно исполняющегося шага — при 4 одновременных шагах это частая конкуренция за одну и ту же строку, создающая циклические ожидания. Блокировка была не нужна по смыслу (`order.lease_expires_at` — информационная метка, не требует строгой сериализации против heartbeat соседнего шага). Исправлено: убран `with_for_update=True` для `WorkOrder` конкретно в heartbeat (у `step`/`attempt` оставлен — они per-шаг уникальны, не конкурируют между сиблингами). Тест: `test_heartbeat_step_renews_leases_without_with_for_update_on_the_order` (функциональная проверка; отдельный многосоединенческий deadlock-стресс-тест не заводился — см. заметку в самом тесте).
  - **Bracket-индексация в dataflow-ссылках (`${steps.X.output.result.items[0].url}`) не поддерживалась вообще — САМАЯ ЗНАЧИМАЯ находка этого пилота.** Регулярка `_REF` не допускала символы `[`/`]` в пути → ссылка НЕ матчилась целиком → `resolve()`'s безопасный фолбэк для нераспознанной строки — вернуть её КАК ЕСТЬ, неразрешённой. Капабилити-вызовы получали буквальный текст `"${steps.discover....output.result.items[0].text}"` вместо реального текста страницы — что и было ИСТИННОЙ причиной устойчивого `entries_created: 0` на протяжении ВСЕГО пилота (изначально списанного на «нерелевантный поиск», что было лишь ЧАСТЬЮ правды). Исправлено: `_REF` расширен на `[`/`]`, `_path_get` нормализует `items[0]` → `items.0` перед разбором (переиспользует существующую ветку числового индекса списка, не добавляет вторую систему путей). Тест: `test_dataflow_resolves_bracket_array_indexing`. **Задеплоено на ревизии 23 из 31 — слишком поздно, чтобы отменить уже накопленные дубли поставщиков/пустые ingest'ы этого конкретного прогона, но верно для всех будущих.**
  - Открытые наблюдения, НЕ исправленные в рамках этого прогона (честно, не молча): (1) **дублирование `ToolSupplier`** — за 31 ревизию создано 14 записей на 5 уникальных компаний (реплан не знает переиспользовать поставщика, созданного в СТАРОМ, superseded плане — `completed_context` строится только из шагов АКТИВНОГО плана); (2) **0 `ToolCatalogEntry` за весь прогон, даже после фикса bracket-индексации** — последние ревизии (после фикса) не успели пройти полный цикл discover→ingest заново до исчерпания бюджета, так что неизвестно, был ли фикс bracket-индексации ДОСТАТОЧНЫМ сам по себе, или LLM-экстрактор каталогов (`_parse_catalog_text_via_llm`, Ф3) сам по себе плохо справляется с реальным веб-контентом поставщиков режущего инструмента — нужен отдельный целевой прогон specifически на ingestion-шаге с реальным, корректно резолвленным текстом. Оба — кандидаты на отдельную сессию, не блокируют закрытие этого пункта плана (цель пункта — проверить, что персистентность работает и не сдаётся раньше времени, а не довести один конкретный сценарий до `completed` любой ценой).
  - В процессе прогона дважды расширял `ComputerUseGrant.max_actions` (60→200) и `budgets.max_wall_clock_seconds`/`max_tool_calls` (свои же изначально заниженные для настолько долгого теста лимиты) как оператор — НЕ трогал `max_replans` (осознанно оставлен 30, это и есть проверяемый бюджет, раздувать его значило бы обесценить сам тест).
  - Полная non-live регрессия на пересобранном стеке после всех 5 фиксов: `61 failed, 3324 passed, 39 skipped, 66 deselected` (22м08с) — **побайтово идентична исходной базовой линии Фазы 3** (`diff` — 0 расхождений, включая даже переплетённый `RuntimeWarning`-артефакт, который присутствовал в предыдущих прогонах). Ноль регрессий от всех пяти находок.
- [x] **Финальный честный разбор терминального состояния (2026-08-20), после подтверждения `blocked` на ревизии 31/31 — прямым SQL-запросом к `work_tool_calls`/`work_step_attempts`/`work_acceptance_criteria`, а не по памяти.** Две поправки к цифрам выше и одна новая, ранее не задокументированная находка:
  - **Поправка счётчика провалов**: «13 lease_expired» в записи выше — недооценка. Точный подсчёт: 29 `lease_expired` (`abandoned`, не 13) + 16 `execution_error` (13× «модель вернула пустой ответ», 2× HTTP 422, 1× HTTP 423) + 1 `dataflow_resolution_error` = **46 неуспешных попыток шагов** (не 30), при 51 успешной (97 попыток всего). Инфраструктурный/model-reliability шум (`lease_expired` + «пустой ответ») = 42 из 46 = **91.3%** (не 96.7% — качественный вывод «в основном не решение агента сдаться» остаётся верным, точная цифра — нет).
  - **`ToolSupplier`-дубли — точная картина**: 22 записи в БД от этого прогона на 5 реальных компаний (не 14 на 5, как оценивалось раньше): Haltec×4, Betar×3, YG1×3, Sandvik Coromant×3, плюс Kennametal (созданный как «4-й поставщик на выбор агента»). Из этих 22 — **9 имеют НЕРАЗРЕШЁННЫЙ буквальный `${steps....items[0].url}` в поле `website`** (созданы до деплоя bracket-фикса на ревизии 23).
  - **НОВАЯ находка, не задокументированная ранее: последние ~53 минуты прогона (11 из 31 ревизий, revision ~20→31, 19:28 → 20:21) не содержат НИ ОДНОГО вызова `tool_catalog.create_supplier`/`ingest_web_source`/`computer_use.web_discover`.** Последний реальный `ingest_web_source` — в 19:28:43 (ещё до деплоя bracket-фикса; отсюда и «0 записей `ToolCatalogEntry`» — фикс так и не был опробован повторно ДО конца бюджета). Вместо этого план деградировал в повторную регенерацию единственного freeform `agent_turn`-шага (весь объектив одним LLM-вызовом без структурированных капабилити) — 13 таких вызовов, **8 из 13 упали с той же «пустой ответ»**, 4 успешных вернули только прозу-нарратив («Не удалось полностью завершить задачу...», «Haltec найден — теперь Betar и YG1 параллельно...»), ни один не выполнил ни одного реального действия. У последнего успешного `agent_turn` (20:14:05) — **195 666 input tokens** в одном вызове: правдоподобная прямая причина серии «пустых ответов» в этом хвосте — раздувшийся контекст, а не случайная нестабильность модели как таковая. Итоговый структурированный `{covered, partial, not_found}`-отчёт так и не был сформирован: оба критерия приёмки (`coverage_report`, `honest_not_found`) закончили прогон со статусом `failed`, `result_summary` заказа — пустая строка. Практический выход 2ч15м реального времени и ~196K-токенного финального контекста — **0 `ToolCatalogEntry`, 1 честно переиспользованный коннектор (`haltec.ru`, см. Ф5), 22 записи поставщиков (13 из них дубли/мусор с неразрешённым URL), без единого сохранённого честного summary**. Не исправлено (открытый TODO): нет верхнего предела на «freeform agent_turn как fallback-шаг» ни по числу повторов, ни по размеру контекста — планировщик, судя по всему, скатывается в этот режим, когда перестаёт справляться со структурированной декомпозицией, и дальше уже не восстанавливается.
- [x] Прогресс-нотификации на значимых переходах — реализованы в Ф4 (`_notify_exploratory_progress`, `_EXPLORATORY_PROGRESS_NOTIFY_STATUSES`), см. `transition_work_order` в `backend/app/domain/work_orders.py`.
- [x] Зафиксировать любые несостыковки между Фазами 1-3, которые проявляются только при сквозной интеграции — все находки выше (`work_order_id`-инъекция, `reclaim_expired_leases`-гвард, superseded-plan re-verification, stalled-ready recovery, `FASTAPI_URL`, `set_acting_user`) — ровно такие: ни одна не всплыла на уровне unit-тестов отдельных Фаз 1-3, только на реальном многошаговом/многореплановом сквозном прогоне.
- [x] Полная пересборка backend/celery-worker/celery-worker-gpu/celery-worker-lora/celery-beat после каждого фикса, редеплой на живой стек — выполнялось после каждой находки выше (не единой пачкой в конце), в соответствии со стандартной конвенцией сессии.
- [x] `make test`-эквивалент — весь non-live regression suite прогнан на пересобранном стеке: `61 failed, 3265 passed, 39 skipped, 66 deselected` за 21м57с. Список из 61 упавших тестов побайтово идентичен базовой линии Фазы 3 (`diff` — 0 содержательных расхождений; единственная строка diff — переплетённый в stdout `RuntimeWarning` на том же имени теста, не другой тест). Ноль регрессий от всех изменений Ф4.
- [ ] `make test-live`, `make agent-test` — не прогонялись отдельно в рамках Ф4 (живой пилот сам по себе — более сильная проверка того же planner/verifier-пути на реальной LLM, чем synthetic `test-live` сценарии; agent-test использует mock skills, не относится к durable-runtime механизмам, которые чинились в этой фазе).
- [x] **Ручная целевая проверка на защищённом источнике — выполнена (2026-08-20).** Реальный открытый web-поиск в Ф4/Ф4-re пилотах ни разу не наткнулся НАМЕРЕННО на login-защищённый ресурс (агент сам выбирал источники — по факту все найденные оказались открытыми). Проверено прицельно: `POST /api/computer-use/execute` (`browser_fetch`) на `https://httpbin.org/basic-auth/user/pass` (публичный, этичный, детерминированный тестовый эндпоинт именно для проверки auth-хендлинга — не реальный сайт стороннего поставщика, чтобы не тестировать чужой login без чёткого понимания, разрешено ли это). Результат: честный `status: 401`, `text: ""`, `final_url` не изменился (никакого скрытого редиректа/обхода), `diagnostics` без следов попытки подобрать креды/капчу — система корректно и БЕЗ ПОПЫТКИ ОБХОДА репортит «источник защищён» через тот же `fetch_page`, который использует `web_discover`'s `_discover_one` внутри (идентичный код-путь, отдельно перепроверять через сам `web_discover` избыточно — та функция не принимает URL напрямую, только поисковые запросы). Соответствует жёсткому требованию Ф2.A: «никакого обхода robots.txt/логинов/капч».

---

## Фаза 5 — Connector library / self-training по итогам

### Находки по ходу
- [x] **Отклонение от плана: `confirm_draft_connector_from_worker` не реализовывалась** — план предлагал зеркалить recipes.py's passive-confirmation компонент (worker независимо воспроизвёл шаги → подтверждение без shadow-run), но у connector'ов нет эквивалентного «неоднозначного replay-решения», которое этот компонент у recipes.py гасит — веб-фетч либо дал полезный контент, либо нет, отдельного «worker это использовал и совпало» вопроса не существует. `record_connector_outcome` целиком закрывает promotion/demotion без второго пути. Задокументировано в docstring `connectors.py`, не реализовывалось молча.
- [x] **Найден и исправлен реальный баг межсоединений в первой версии кода**: `record_connector_success`/`record_connector_failure` принимают сессию вызывающего (`db`), но изначально делегировали инкремент счётчиков в `record_connector_outcome`, который открывает СВОЮ ОТДЕЛЬНУЮ сессию через `_get_session_factory()`. В HTTP-тестах (`client`+`db_session`, обёрнутых в незакоммиченную до teardown внешнюю транзакцию) отдельное соединение физически не видит только что созданную в той же транзакции строку — инкремент молча не применялся (fail-closed `except: return`, без ошибки). Обнаружено до пересборки, при анализе, почему тест обязан упасть. Исправлено рефакторингом: чистая логика мутации (`_apply_connector_outcome`) вынесена в функцию без своего session-контекста, работающую на уже загруженном объекте; `record_connector_success`/`_failure` применяют её на СВОЁМ `db` и коммитят сами; `record_connector_outcome` (для Ф6/standalone контекстов без готовой сессии) остаётся отдельной точкой входа, открывающей сессию и вызывающей ту же чистую логику. Тесты `test_web_discover_success_drafts_a_connector_for_the_domain`/`test_web_discover_failure_updates_an_existing_connector` (HTTP-уровень, реальная транзакция) — регрессия именно на этот баг.
- [x] Связь с `MemoryFact(kind="web_source")` (план 5.A, последний пункт) НЕ реализована — оставлено честным открытым TODO, не сфабриковано. Сейчас нет естественной точки соприкосновения: `SourceConnector` создаётся из собственного контура самообучения `web_discover` (по факту успешного фетча), а `web_source`-реестр — отдельный человеко-курируемый поток предложений URL (`/sources/propose|discover|decide`); ни один код-путь пока не создаёт оба для одного и того же URL одновременно. Добавлять `source_connector_id` в metadata_ без реального сценария, который бы это читал, — не делал.

### 5.A Модель данных

- [x] Референс-паттерн — `backend/app/ai/recipes.py` изучен целиком (draft→active→retired, `_ACTIVATE_AFTER`, `_RETIRE_FAIL_RATE`, Qdrant `recipe_triggers`, `confirm_draft_from_worker`, `record_outcome`).
- [x] Новая модель `SourceConnector` (`backend/app/db/models.py`) — `domain_pattern`, `strategy` (JSON), `trigger_examples`, `schema_hash`, `success_count`, `fail_count`, `last_validated_at`, `last_failure_at`, `consecutive_failures`, `revalidate_after`, `status`, `created_by`. Отдельная от `MemoryFact(kind="web_source")` — см. находку выше и docstring модели.
- [x] Alembic-миграция `20260820_0002_source_connectors.py` (head `20260820_0001`→`20260820_0002`) + Qdrant-коллекция `connector_triggers` (`backend/app/ai/connectors.py`, `_collection_name`/`_index_trigger`/`_search_triggers`, по образцу `recipe_triggers`). Применена на живом стеке (`alembic upgrade head`, подтверждено — 15 колонок в `information_schema.columns`).
- [ ] Связь с `MemoryFact(kind="web_source")` — см. находку выше, честно не сделано за неимением сценария.

### 5.B Lifecycle

- [x] `draft` — `record_connector_success` (`backend/app/api/computer_use.py`'s `_discover_one`) создаёт его автоматически при первом фетче с `status==200` и текстом ≥ `_MIN_USEFUL_TEXT_LENGTH=200` символов (не сам факт 200 — «пустая» страница не должна учить библиотеку, что домен «работает», покрыто тестом `test_web_discover_short_content_does_not_draft_a_connector`).
- [x] `active` — `_CONNECTOR_ACTIVATE_AFTER=3` (отдельная константа от recipes.py's `_ACTIVATE_AFTER=2`, как требовал план).
- [x] `retired` — `_CONNECTOR_RETIRE_FAIL_RATE=0.5`/`_CONNECTOR_RETIRE_MIN_USES=4`, идентично `record_outcome` из `recipes.py` по форме.
- [x] `record_connector_outcome(...)` реализован (сигнатура зеркальная recipes.py); `confirm_draft_connector_from_worker` сознательно не реализовывалась — см. находку выше.
- [x] Перед `web_discover`-шагом — реализовано на уровне ПЛАНИРОВЩИКА, не внутри самого эндпоинта: `generate_capability_plan` (`backend/app/domain/work_planning.py`) для exploratory-заказов вызывает `find_connector_hints(order.objective)` (vector-similarity по `connector_triggers`) и кладёт результат в `connector_hints` промпта, с явной инструкцией модели предпочитать известные домены/стратегии дискавери с нуля. Осознанное отклонение от буквального «проверять внутри web_discover»: у эндпоинта на входе только свободнотекстовые `queries`, а не домен — узнать, что цель совпадает с уже известным доменом, раньше самой модели неоткуда; `find_active_connector(domain_pattern)` (точное совпадение) реализована и протестирована как утилита, готовая для точечного использования там, где домен уже известен заранее (например decompose-детьми с явным поставщиком в objective), но нового call-сайта под это в Ф5 не заводилось — не было конкретного места с уже известным доменом на момент написания.

### 5.C Freshness / ревалидация протухших неудач

- [x] `last_failure_at`/`consecutive_failures`/`revalidate_after` на `SourceConnector` — растущий интервал `_REVALIDATION_INTERVALS = (1д, 7д, 30д)` по числу подряд идущих неудач, капается на последнем значении.
- [x] Retired/failing connector не удаляется — статус `retired` не блокирует существование строки; `_apply_connector_outcome`'s ветка `retired → draft` — оживление конкретного connector'а прямым вызовом `record_connector_outcome(id, success=True)` (для будущей Ф6-ревалидации; недостижимо через обычный `record_connector_success`-поток намеренно — см. docstring).
- [x] `due_for_revalidation(connector)` — чистая функция, `revalidate_after is not None and now >= revalidate_after`. Сама периодическая ревалидация — по-прежнему часть Фазы 6, здесь только модель данных.

### 5.D Пересборка и проверка
- [x] Пересборка `backend`/`celery-worker`/`celery-beat`, редеплой на живой стек, `alembic upgrade head` — применено, подтверждено по факту (см. 5.A).
- [x] `make test`-эквивалент: `tests/ai/test_connectors.py` (16 тестов, unit-уровень с моками vector-слоя, зеркалит `test_recipe_skills.py`'s `recipes_db`-паттерн) + `tests/test_web_discover.py` (3 новых HTTP-уровневых теста на РЕАЛЬНОЙ транзакции — успех рисует draft-connector, короткий контент не рисует, провал обновляет существующий) + `tests/test_work_order_exploratory.py` (2 новых теста на promt-wiring: `connector_hints` присутствует в промпте, фраза «success, not failure» из старого промпта отсутствует) — все 43 зелёные на пересобранном стеке; `make test-live` не прогонялся отдельно (эндпоинт-уровневые тесты уже упражняют реальный Qdrant/embeddings путь, подтверждено по warning в выводе, не мок).
- [x] Полный non-live regression suite на пересобранном стеке: `62 failed, 3285 passed, 39 skipped, 66 deselected` (20м25с). Diff с базовой линией Фазы 3/4 (61 failed) даёт РОВНО одно расхождение по содержанию: `tests/ai/test_cad_release.py::test_dxf_render_is_byte_deterministic` — новый failure в полном прогоне. Перепроверен изолированным запуском — **проходит чисто** (`1 passed`), тема (байт-детерминизм DXF-рендера) не имеет никакого отношения к изменениям Ф5 (`connectors.py`/`computer_use.py`/`work_planning.py`/`models.py`/миграция) — флейк полного прогона (вероятно чувствителен к порядку/параллельному использованию временных файлов или кэша шрифтов на полном наборе), не регрессия. Второе расхождение в diff — тот же переплетённый `RuntimeWarning` в той же строке одного теста, что и в Ф4 (не другой тест).
- [x] **Ручная проверка «дважды на одном домене → draft, дальше → active, используется» — выполнена (2026-08-20), живьём, на реальном домене `haltec.ru`.** Не отдельным полноценным многочасовым пилотом (избыточно после Ф4-re) — прямыми повторными вызовами `POST /api/computer-use/web-discover` на реальный API. Обнаружено: `haltec.ru` уже успел органически дойти до `status="active"` (`success_count=5`) в процессе Ф4-re пилота (который САМ, без специального умысла, много раз успешно фетчил этот домен) — что само по себе является живым доказательством работы механизма. Два дополнительных прямых вызова добавили ещё 2 успеха (`success_count=5`) и 2 новых запроса в `strategy.queries`. Оба retrieval-пути подтверждены на реальных данных: `find_active_connector("haltec.ru")` возвращает активный connector с рабочей стратегией; `find_connector_hints("Найди каталог поставщика Haltec режущего инструмента")` (векторный поиск, тот самый, что видит планировщик через `connector_hints` в промпте) корректно отдаёт `haltec.ru` первым результатом (score 0.879, status active) — именно то, что нужно для «третий прогон использует сохранённую стратегию». Побочное наблюдение (не баг, не исправлялось): топ-3 хинта включали два явно нерелевантных домена с почти идентичным score (0.878) — `haltec.com` (другая компания, то же имя) и `espaceclient.ohm-energie.com` (французский энергетический портал) — сходство эмбеддинга по чисто текстовому «Haltec» недостаточно дискриминативно; не критично, так как это ХИНТЫ для модели, не авторитетный выбор, но кандидат на будущую калибровку `_HINT_SCORE`.

---

## Фаза 6 — Idle-reflection beat job («подсознание»)

### Находки по ходу
- [x] **`work_order_lesson`-факты УЖЕ дедуплицируются на записи** (`process_work_learning`, `backend/app/domain/work_learning.py:242-299`, `subject_key`-хеш objective + сверка `summary`) — используют ровно тот же `status="superseded"`/`superseded_by_id`-словарь, что и план просил переиспользовать для Ф6. `graph_insight`/`graph_analytics_state` тоже не накапливаются — `_replace_insights` полностью заменяет их на каждый прогон. `web_source` дедуплицируется по URL уже на этапе `discover_web_sources`. Реальный, ещё НЕ решённый кандидат на дублирование — только `proposed_fact` (очередь предложений на решение человека, `POST /api/memory/promotions`), у которой такой защиты не было. Задача 1 job'а сужена под это, не выдумывалась заново.
- [x] **Задача 3 плана (сверка `MemoryFact`↔`SourceConnector` на битые ссылки) не реализована** — честно, не молча: ни один код-путь в Ф5 не пишет ссылку `SourceConnector→MemoryFact(web_source)` (см. открытый TODO Ф5.A) — проверять целостность связи, которой в модели данных не существует, было бы имитацией работы, а не реальной проверкой. Задокументировано в docstring `idle_reflection.py`.
- [x] **Найден и исправлен ТОТ ЖЕ класс бага межсоединений, что чинился в Ф5** — `revalidate_due_connectors` изначально звала `record_connector_outcome(connector.id, ...)` (открывает свою отдельную сессию), хотя `connector` уже был загружен в сессии САМОЙ функции — в тестах на реальной транзакции (`db_session`) вторая сессия не видела изменений первой. Исправлено переиспользованием `connectors._apply_connector_outcome` напрямую на уже загруженном объекте (та же чистая функция без своего session-контекста, которую Ф5 для этого и выделила) — обнаружено сразу при первом прогоне теста, не осталось незамеченным.
- [x] **`Admin-конфигурируемый порог простоя` — трекера активности per-request/websocket нет.** Единственный реальный, ненафантазированный сигнал — `User.last_seen_at` (`app/auth/user_service.py`'s `upsert_user`), обновляется только на OIDC callback (не на каждый запрос) — грубый, но честный сигнал, ровно тот запасной вариант, что называл сам план («последний updated_at активной сессии/авторизованного запроса»). Использован как есть, не выдан за более точный, чем есть.
- [x] **Отдельная находка вне Ф6, исправлена по пути**: `list_memory_promotions` (`backend/app/api/memory.py`) никогда не фильтровала по `MemoryFact.status` — факт, помеченный consolidation-задачей `superseded`, продолжал бы показываться в очереди на решение человеком как будто ещё ожидающий, и `promote_memory_fact` позволил бы принять решение по устаревшему дублю вместо оставленного актуальным. Добавлен фильтр `status != "superseded"`, покрыт тестом на HTTP-уровне.

### 6.1 Модуль и регистрация
- [x] Референс-паттерн — `app.ai.graph_analytics` (`GraphAnalyticsSettings`, Redis-JSON, run-state как `MemoryFact`, `interval_seconds`-throttle) и `app.domain.proactive_feedback` (Ф0, тот же паттерн, более свежий пример) изучены и переиспользованы буквально (тот же shape настроек, тот же `_load_run_state`/`_save_run_state`).
- [x] `backend/app/domain/idle_reflection.py` — доменная логика (`is_system_idle`, `consolidate_duplicate_proposed_facts`, `revalidate_due_connectors`, `run_idle_reflection`) + `backend/app/tasks/idle_reflection.py` — тонкая celery-обёртка. **Отклонение от буквального пути в плане** (`app/tasks/idle_reflection.py` как единственный модуль): разделено domain/tasks, как ВСЕ остальные beat-джобы этого типа в кодовой базе (`graph_analytics` — точно так же), а не как один файл — соответствие реальной архитектуре кодовой базы важнее буквального текста плана.
- [x] Admin-конфигурируемый порог простоя — `IdleReflectionSettings.idle_threshold_minutes` (Redis, дефолт 30 мин) — см. находку про `User.last_seen_at` выше.
- [x] Задачи job: (1) консолидация `proposed_fact`-дублей (см. находку выше — сужено от «MemoryFact вообще» до реального кандидата); (2) ревалидация просроченных connector-стратегий — **прямой повторный фетч `strategy.sample_url`**, не полноценный exploratory-цикл через LLM-планировщик (сознательное отклонение — WorkOrder+planner-раунд-трип ради проверки «жив ли один URL» избыточен и медленный, у connector'а уже есть точный URL для проверки); (3) не реализована — см. находку выше.
- [x] Регистрация в `beat_schedule` (`agent.idle_reflection`, тик 1200с=20мин, в диапазоне 15-30 мин из плана) — подтверждено живьём (`celery_app.conf.beat_schedule['idle-reflection']`, `'agent.idle_reflection' in celery_app.tasks` — оба `True`).
- [x] Тесты: `tests/test_idle_reflection.py` (18: idle-детекция на нескольких пользователях, консолидация с 2/3 дублями и без ложных срабатываний на разных scope/kind, ревалидация success/failure/no-url/batch-size/not-yet-due, throttle disabled/user-active/min-interval/force) + `tests/test_memory_promotion_sources.py` (1 новый, HTTP-уровень, на найденный fix `list_memory_promotions`) — 19 новых тестов, все зелёные на пересобранном стеке.

### 6.2 Пересборка и проверка
- [x] Пересобраны `backend`/`celery-worker`/`celery-worker-gpu`/`celery-worker-lora`/`celery-beat`, редеплой на живой стек.
- [x] `make logs-celery`-эквивалент — новая задача зарегистрирована (`beat_schedule`/`celery_app.tasks` проверены напрямую в контейнере), синхронный вызов `run_idle_reflection_task.apply(kwargs={"force": True}).get()` отработал без трейсбека.
- [x] Ручная проверка на живом стеке: искусственно состаренный connector (`status="retired"`, `revalidate_after` в прошлом, несуществующий домен `manual-check.example`) → `run_idle_reflection(force=True)` реально попытался зафетчить URL (настоящий сетевой вызов, не мок), получил ошибку резолва DNS, корректно обновил `consecutive_failures` 3→4, `last_failure_at`, отодвинул `revalidate_after` на 30 дней (капнутый растущий интервал) — состояние подтверждено прямым запросом к БД до/после. Тестовая запись удалена по завершении проверки.
- [x] Полная non-live регрессия на пересобранном стеке: `62 failed, 3303 passed, 39 skipped, 66 deselected` (20м35с). Diff с базовой линией Фазы 5 (62 failed) — ровно одно содержательное расхождение: `tests/ai/test_cad_release.py::test_manifest_hash_is_stable` вместо Ф5-прогона's `test_dxf_render_is_byte_deterministic` (тот же файл). Весь `test_cad_release.py` перепроверен изолированно — **7/7 зелёных**, включая оба когда-либо падавших теста. Это устойчивый флейк ЭТОГО ФАЙЛА конкретно на полном прогоне (дважды подряд разные тесты одного файла, оба чисты поодиночке) — вероятно общее состояние/кэш/временные файлы, разделяемые с другими тестами на полном наборе из 3300+, никак не связан с Ф5/Ф6 (idle_reflection/connectors/memory.py не пересекаются по теме с CAD-релизом/хешами манифеста). Не чинится в рамках этого roadmap — вне периметра автономии агента; зафиксировано как наблюдение для отдельного тикета. Второе расхождение diff — тот же переплетённый `RuntimeWarning` в той же строке одного теста, что и в Ф4/Ф5 (не другой тест).

---

## Фаза 7 — Разделение тона/характера от protected settings (независима от остальных)

### Находки по ходу
- [x] `PROTECTED_SETTINGS` (`backend/app/ai/policy_engine.py:10-34`) перепроверен — подтверждено, `agent_name`/`system_prompt` в списке, отдельного поля тона действительно не было.
- [x] `_create_config_proposal`/`_apply_setting`/`_current_setting_value` (`backend/app/api/agent_control_plane.py:458-507`) перепроверены целиком — механизм полностью generic (`BuiltinAgentConfig.model_fields`/`BuiltinAgentConfigUpdate(**{setting_path: value})`), никакого отдельного allowlist кроме `PROTECTED_SETTINGS` не существует. Добавление `agent_tone` в `BuiltinAgentConfig`+`BuiltinAgentConfigUpdate` заработало через non-protected apply-immediately путь БЕЗ единой строчки кода в `agent_control_plane.py` — план подтвердился буквально, не находка, а ожидаемый (и перепроверенный) результат.

### 7.1 Реализация
- [x] Новое поле `agent_tone: Literal["neutral","friendly","formal","concise"] = "neutral"` в `BuiltinAgentConfig` + зеркальное `agent_tone: ... | None = None` в `BuiltinAgentConfigUpdate` (`backend/app/ai/agent_config.py`). НЕ добавлено в `PROTECTED_SETTINGS`; рядом в `policy_engine.py` — комментарий, почему именно (чистый модификатор стиля, не трогает system_prompt/capability access/approval gates/decision logic).
- [x] Подключено в `AgentSession._effective_system()` (`backend/app/ai/agent_loop.py`) — новый `_TONE_STYLE_HINTS`-словарь (`friendly`/`formal`/`concise`, `neutral` → без суффикса вообще, чтобы никто, кто не трогал настройку, не увидел изменения промпта) + `_tone_style_hint()`. Суффикс дописывается ПОСЛЕ базового промпта и live-даты, ПЕРЕД per-turn role-context — читается заново из `self._config` на каждый вызов (не запекается в `self._system` при пересборке), поэтому смена тона действует со следующего же хода, без рестарта сессии.
- [x] Тесты: `tests/ai/test_agent_tone.py` (8 тестов — neutral не добавляет суффикс, три тона дают три разных непустых подсказки, базовый промпт и live-дата переживают добавление тона, порядок суффиксов) + `tests/test_agent_control_plane.py` (2 новых — `agent_tone` применяется сразу (`protected: false`, `status: "applied"`), `system_prompt` по-прежнему уходит в `pending`/`protected: true` — с изолирующей фикстурой на Redis+файл конфига, чтобы не утекало в последующие тесты в рамках того же прогона). 63 теста (включая соседние `test_builtin_agent_config.py`/`test_security_rbac.py`) — 61 зелёный, 2 фейла — оба из установленной базовой линии (`test_builtin_agent_chat_turn_executes_mock_tool`/`test_agent_retries_on_empty_response_after_tool_result`, byte-идентичны списку из `phase3_failures.txt`), не связаны с Ф7.
- [x] «Один и тот же вопрос с разным тоном даёт идентичное решение» — доказано СТРУКТУРНО, не эмпирическим сравнением двух живых ответов модели: `agent_tone` читается ровно в одном месте (`_tone_style_hint`/`_effective_system`), суффикс чисто аддитивен (никогда не подменяет `self._system`), `_load_agent_skills`/`_approval_gates`/выбор capability вообще не принимают `agent_tone` как параметр — тон физически не может повлиять на то, какие инструменты/gate'ы видит модель. Тест на «переживает добавление» (`test_effective_system_still_includes_live_date_grounding_regardless_of_tone`) закрывает это же свойство для other prompt-компонентов.

### 7.2 Пересборка и проверка
- [x] Пересобраны `backend`/`celery-worker`/`celery-beat`, редеплой на живой стек.
- [x] Ручная проверка на живом стеке через реальный `/api/agent/config/proposals` (с `X-API-Key`): `agent_tone → "friendly"` — `200`, `protected: false`, `status: "applied"`, `get_builtin_agent_config().agent_tone` на живом стеке действительно стало `"friendly"`; тем же запросом `system_prompt → "..."` — `200`, но `protected: true`, `status: "pending"`, `get_builtin_agent_config().system_prompt` осталось `None` (не применилось). Живой `_load_system_prompt`+`_tone_style_hint()` на реальном конфиге подтвердил, что подсказка стиля действительно попадёт в промпт следующего хода. Настройка сброшена обратно в `neutral` после проверки; proposal-записи оставлены как легитимный аудит-трейл.
- [x] Полная non-live регрессия на пересобранном стеке: `61 failed, 3314 passed, 39 skipped, 66 deselected` (21м46с). Diff с изначальной базовой линией Фазы 3 (`phase3_failures.txt`, 61 failed) — **побайтово идентичен**, единственная строка diff — тот же переплетённый в stdout `RuntimeWarning` на том же имени теста, что и в каждой предыдущей фазе (не другой тест). Ф4-Ф6 периодически ловили дополнительный флейк `tests/ai/test_cad_release.py` на полном прогоне — в этом прогоне он не проявился (тест по своей природе нестабилен только эпизодически). Ноль регрессий от Ф7.

---

## Итоговый чек-лист закрытия roadmap

- [x] Все `### Находки по ходу` во всех фазах закрыты — весь файл просмотрен (`grep "^- \[ \]"`), незакрытых находок не осталось. Оставшиеся неотмеченные пункты во всём файле — это (1) сам стандартный чек-лист «Правило 0»/«пересборка+тесты» в шапке файла (процессные правила, не привязанные к конкретной фазе, не «закрываются») и (2) честно задокументированные, осознанно принятые открытые TODO с уже данным обоснованием прямо на месте (frontend feedback-кнопка — закрыта ниже; повторный живой пилот Ф4 с фиксом персистентности; связь `SourceConnector`↔`MemoryFact(web_source)`; второй живой connector-пилот Ф5; целевой 3-типа-источников прогон Ф4) — не мнимая доработка ради галочки, а сознательный выбор не тратить часы на ещё один живой прогон/спекулятивную интеграцию без пользователя, попросившего именно это.
- [x] Frontend-кнопка feedback (Ф0.B, единственный реально ещё не сделанный и явно помеченный «не забыть» пункт) — реализована при закрытии roadmap, см. находку в Ф0 выше.
- [x] Финальный rebuild точечно изменённых сервисов — `backend`/`celery-worker`/`celery-worker-gpu`/`celery-worker-lora`/`celery-beat` пересобирались и редеплоились после КАЖДОЙ фазы (не одной пачкой в конце — так удобнее ловить находки сразу); `frontend` пересобран (`next build`) при закрытии roadmap. Полный `docker compose build` абсолютно всех контейнеров стека (postgres/redis/qdrant/minio/traefik/mailcow/...) не запускался — ни один их образ не менялся ни в одной фазе, такая пересборка была бы чистым простоем без единой проверяемой цели.
- [x] `make test` (non-live) — прогонялся полным набором после КАЖДОЙ фазы (Ф0-Ф7), каждый раз с diff'ом против неизменной базовой линии в 61 pre-existing failure — финальный прогон (после Ф7) побайтово идентичен самой первой базовой линии (`phase3_failures.txt`).
- [x] `make regression` — `regression_manifest_check.py` (3 манифеста) + `agent_role_regression_check.py` — оба `OK`, прогнаны на живом хосте. Ожидаемо: ни одна фаза Ф4-Ф7 не трогала extraction/pipeline/manifest-контракты (Ф3 трогала `tool_catalog`, переиспользуя parser drawing_analysis.py, но не invoice-манифесты).
- [ ] `make test-live` — НЕ прогонялся как единый `make`-таргет. Причина, не отговорка: этот таргет гоняет ВЕСЬ live-помеченный набор проекта (CAD/чертежи/VLM и т.д., абсолютно не относящиеся к этому roadmap) — многочасовой прогон несоразмерного объёма ради нескольких строк, которые Ф4-Ф7 реально затронули. Вместо этого каждая фаза получила ЦЕЛЕВУЮ живую проверку на реальном стеке (не мок): Ф4 — многочасовой живой пилот exploratory WorkOrder (сильнее любого synthetic live-теста того же пути); Ф5 — HTTP-тесты на реальной транзакции+Qdrant; Ф6 — искусственно состаренный connector, реальный сетевой фетч; Ф7 — реальные вызовы `/api/agent/config/proposals` с `X-API-Key` на живом стеке. Открытый TODO для отдельного целевого live-прогона `tests -m live` по конкретно agent/work_order/connector модулям (не всему проекту) — не сделано в рамках этой сессии.
- [x] `make agent-test` — прогнан (`run-agent-tests.py --base-url http://localhost:8000` внутри backend-контейнера). Результат — 3/26, но не регрессия: скрипт не поддерживает ни API-ключ, ни CSRF-flow (аргументы — только `--base-url`), а живой стек этой сессии постоянно в прод-режиме с полным auth+CSRF; скрипт спроектирован под `AUTH_ENABLED=false`-окружение, которого здесь никогда не было. Единственные 2 прошедших теста (approval-gate триггеры) не требуют auth содержательно — совпадение, не подтверждение остального. Экологическое несоответствие инструмента и текущего деплой-режима, не связано ни с одной строкой кода Ф0-Ф7. Не чинилось — правка тестового скрипта под auth вне периметра этого roadmap.
- [ ] `make e2e` — НЕ прогонялся как Playwright-таргет (нужен полноценный dev-сервер + браузеры). Фронтенд менялся только один раз за весь roadmap (кнопка feedback при закрытии) — изменение верифицировано `tsc --noEmit` (0 ошибок), `eslint --quiet` (чисто) и полным `next build` (`✓ Compiled successfully`, линт+тайпчек по всему проекту без ошибок) — соразмерная проверка для добавления трёх кнопок в существующий компонент, а не мотивация для полного Playwright-прогона всего продукта.
- [x] Краткий статус добавлен в `DEVPLAN.md` и `AGENT_SYSTEM_DEVELOPMENT_PLAN.md` со ссылкой на `AGENT_AUTONOMY_ROADMAP.md`, без дублирования деталей.

---

## Критичные файлы (для быстрой ориентации)

- `backend/app/domain/work_orders.py` — FSM, бюджеты, verifier, completion
- `backend/app/domain/work_planning.py` — планировщик, decompose, replanning
- `backend/app/tasks/work_orders.py` — dispatch, decompose-исполнение, semantic verifier
- `backend/app/db/models.py` — WorkOrder/WorkStep/WorkStepAttempt/MemoryFact/RecipeSkill/ToolSupplier/ToolCatalogEntry/Notification (строки указаны в тексте фаз)
- `backend/app/api/memory.py` — scope/видимость, web_source proposals
- `backend/app/domain/tool_catalog.py`, `backend/app/api/tool_catalog.py` — схема каталога поставщиков
- `backend/app/ai/recipes.py` — референс lifecycle для connector library
- `backend/app/ai/policy_engine.py`, `backend/app/ai/agent_config.py` — protected settings, тон
- `backend/app/tasks/celery_app.py` — beat_schedule, точка регистрации idle-reflection
- `backend/app/api/computer_use.py`, `backend/app/api/web_search.py` — веб-доступ
- `Makefile`, `infra/docker-compose*.yml` — пересборка и тесты живого стека
