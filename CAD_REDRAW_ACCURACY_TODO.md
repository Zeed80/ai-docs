# Доработка «Оцифровка в DXF → Перечертить по чертежу»

**Статус:** выполняется последовательно.  
**Контрольный лист:** `test_vector_files/detal_126.png`.  
**Цель:** доказуемая цепочка `чертёж → чтение → нормализация → 3D → необходимые 2D-виды → DXF` без рамки и без ложного статуса точности.

Обозначения: `[x]` завершено и проверено, `[-]` выполняется/частично, `[ ]` не начато.

## Зафиксированный baseline 2026-07-28

- [x] Живой production-прогон метода `spec`, профиль `mechanical`, три прохода чтения.
- [x] Зафиксированы вход, JSON чтения, CadIR, DXF, STL и визуальные рендеры результата/эталона.
- [x] Зафиксирован основной дефект: модель построила сплошной вал `364 × Ø90` вместо полого шпинделя `470 × Ø102`; потеряны конус 7:24, внутренний профиль, два шпоночных паза, три поперечных отверстия и две фаски.
- [x] Зафиксирован ложноположительный внутренний статус `solid_3d.verification.ok=true`: он сравнивал B-Rep только с уже ошибочно прочитанным габаритом.
- [x] Зафиксированы дефекты листа: неверный набор видов, нет требуемого продольного разреза и штриховки, дубли текста размеров (`1717`, `Ø9090`).
- [x] Финальная приёмка человеком заблокирована; однако задача и 3D-верификатор передают противоречивые сигналы успеха.

## Наблюдаемость и timeout-контракт 2026-07-30

- [x] Для назначений «Чтение чертежа», «Текстовый слой» и «Чертёжник» возвращён tri-state переключатель reasoning: `модель | вкл | выкл`; CAD reader/drafter/OCR по умолчанию работают с явным `thinking=false`.
- [x] Ollama получает одновременно `think=false` и `chat_template_kwargs.enable_thinking=false`, чтобы Qwen/Apex не складывал весь структурированный ответ в thinking-поле.
- [x] Whole-sheet ответ ограничен 6000 токенами вместо 24000; всё чтение имеет внутренний предел 480 секунд, оставляющий время на сохранение честного terminal status до Celery soft limit 600 секунд.
- [x] `SoftTimeLimitExceeded` и исключения вне coroutine переводят generation/job в `failed`, а не оставляют запись навечно в `running`.
- [x] В `params.cad_process` по мере выполнения сохраняется нумерованный журнал: источник, нормализация, manifest/model/thinking, каждый fragment-вопрос, OCR, каждый whole-sheet проход, follow-up, cross-check, dimension graph, feature tree gate, SHA входа ядра, compile, B-Rep и 2D-лист.
- [x] Добавлен `GET /api/image-gen/{id}/cad-process`; тот же журнал виден во время выполнения/ошибки в Studio и после построения в CAD workspace, включая токены, длительность, модель, размер ответа и точный последний незавершённый этап.
- [x] Живой replay `f0269d50-7dd3-4701-823f-048c9ccf3a64` доказал, что прежний whole-sheet контракт трижды обрывался на 6000 токенах. Контракт разделён: whole-sheet возвращает только геометрию/виды, а размеры, примечания и штамп берутся из fragment/OCR; неполный ответ теперь сохраняет preview/tail, token counts и `done_reason`.
- [x] Контрольный replay `eda72fd2-a335-40ec-84e3-d2e7d1d46041` завершил три whole-sheet прохода валидным JSON (`497` output tokens, `1173` символа, `thinking_chars=0`) без timeout. Все проходы одинаково прочитали диаметры, но не локализовали длины ступеней; feature tree и CAD-kernel честно заблокированы, STL/DXF не созданы.
- [x] Добавлен детерминированный локализатор горизонтальных размерных линий и выносок. На `detal_126.png` он независимо связал габарит `470`, левые станции `17`, `78`, `150`, `240` и размеры от правого торца `99 → x=371`, `270 → x=200`; OCR `50` исправляется в `150` только по независимо прочитанной выноске и согласованному пролёту.
- [x] Fragment-reader получает локализованные bbox/линии/станции в prompt и сохраняет их в `reader.axial_dimensions`. Станция из ответа модели, которой нет в локализованном evidence, теперь отклоняет проход: replay `486d6b0b-77fe-4535-87df-be3761c2397b` трижды заблокировал выдуманную станцию `300`, перешёл к whole-sheet fallback и не вызвал CAD-kernel.
- [x] Удалены геометрические placeholders: несколько непрочитанных ступеней больше не получают равные доли габарита, а при отсутствии габарита — среднюю длину. Допускается только единственная неизвестная длина, строго выводимая вычитанием из указанного габарита; неоднозначная геометрия остаётся `unresolved`.
- [x] Финальный production replay `9b130d46-0b15-4d12-9397-8333695555a6` после пересборки подтвердил контракт: `88` событий журнала, `assumptions=0`, семь непрочитанных наружных длин сохранены как `null`, статус `failed/blocked`, `solid_input=null`, STL/DXF отсутствуют. Пользователь получает конкретную причину остановки и может открыть сохранённое чтение; ложных длин `470/7 = 67.143 мм` больше нет.
- [x] Диаметральные выноски привязаны к измеренному цветоразделённому контуру и классифицированы как `outer | bore`; монохромный или пространственно неоднозначный вход остаётся `unresolved`. На живом replay `1a165c52-083f-4c7b-a46b-1ff583e55751` три прохода независимо подтвердили наружный профиль `Ø102×14 + Ø80×357 + Ø72×99`, внутренний профиль по станциям `78/150/200/352/395/445/470` и конус `7:24`. Whole-sheet fallback больше не может заменить эти профили ответом модели.
- [x] Локализованы оба контура шпоночных пазов: `85×12` при `z=276,71` и `35×8` при `z=370,981`. Глубина первого `5` связана с локальной выноской главного вида; глубина второго `3,5` — с геометрически согласованным вынесенным сечением `Ø72`. Шероховатость `3,2` и радиус `R4` больше не могут быть приняты за глубину паза. Consensus сохраняет подтверждённые feature-объекты и их bbox-evidence.
- [x] Финальный replay этапа `f75b877a-3c0a-428d-93a5-2f1013688c75` повторил оба паза во всех трёх проходах и сохранил доказанные outer/bore/keyways после whole-sheet fallback. В итоговом чтении нет устаревших `length-missing`; остались только восемь точных blocker-ов правого узла. `solid_input` не создан, CAD-kernel не вызван, ложной гладкой 3D-модели нет.
- [x] Правый узел читается отдельным увеличенным вопросом поверх детерминированных осей. Живой promotion replay `bb4328a8-6147-4f53-9eb8-9713e7ed3998` после production rebuild и трёх проходов сохранил верхнее `Ø14` до расточки при `z=409,978`, верхнее `Ø10` до расточки и нижнее `Ø9` до расточки при `z=455,079`. Строгая проверка запрещает модели переносить `Ø9` на контур, независимо измеренный как `Ø14`; уникальные OCR-bbox `Ø10`/`Ø9` задают стороны `0°/180°`. Consensus сохраняет одинаковые boolean и полную ступенчатую структуру отверстия.
- [x] `Ø24` классифицирован как цековка глубиной `3 мм` над верхним `Ø10`, а не как четвёртое поперечное отверстие. Контракт и 3D feature tree несут `counterbore_diameter_mm/counterbore_depth_mm` и компилируют отдельный неглубокий радиальный cut. В том же promotion replay внутренняя резьба `M54,5×2` однозначно привязана к измеренной расточке `Ø55`; непривязанной честно осталась только `M75×1,5`.
- [x] Каждая наружная ступень, цилиндрическая расточка и конус теперь передают в 3D собственный source-bbox и текст provenance из цветоразделённого контура. Живой replay `7675e10d-7aec-418c-b3b2-939344eca725` сохранил evidence на всех `3/3 outer` и `7/7 bore`; скрытый gate-blocker `геометрия без локализованного evidence` исчез. В `solid_input` пользователь видит точный feature tree и SHA даже при заблокированном вызове ядра.
- [-] Повторная визуальная сверка `detal_126.png` выявила ошибку прежнего эталона: два M8 расположены на PCD `Ø80`, а `Ø65` относится к отдельной группе `8 отв. Ø1`. Вход M8 находится на утопленной плоскости, измеренной по векторному контуру как `5,6 мм`; рядом стоящее «6 фасок» больше не используется как ложный линейный размер. Размеры резьбы/сверления остаются `15/17 мм`. Для корректного B-Rep дополнительно нужен неуказанный/непрочитанный Ø самой входной выборки: без него M8 теперь тоже не строится. Читатель различает эти факты, наружный контур `Ø98×13` больше не поглощается соседним `Ø80`. Дополнительно сохранены группы `8×Ø1` на `Ø65` под `45°` и `12×Ø4` на `Ø70` с размерами `18/82`; пока их торец и угловая фаза не доказаны, они видимы пользователю и намеренно блокируют сборку вместо построения по умолчанию. Не локализованы все шесть фасок.

## P0. Запрет ложного успеха

- [x] Разделить недостающие данные 3D-кандидата на критические блокеры и предупреждения.
- [x] Не вызывать CAD-ядро, если не доказаны критические элементы детали: общий габарит, замкнутый наружный профиль, внутренний профиль при наличии разреза, положения/размеры обязательных вырезов.
- [x] Запретить безусловное `confirm_assumptions=true` для режима чтения чертежа.
- [x] Возвращать единый статус `blocked | built_unverified | verified`, не смешивая `ok=true`, `review_required` и `verification.ok=true`.
- [x] Проверять не только envelope, но и соответствие полного списка заказанных и реально выполненных feature-операций.
- [x] Любая неудавшаяся операция ядра (`hole`, `keyway`, `chamfer`, `bore`) делает геометрическую верификацию отрицательной.

## P1. Прозрачность «что прочитано → что передано в 3D»

- [x] Сохранять все попытки чтения, consensus, нормализованную спецификацию, граф размеров, feature tree и точный kernel payload. Для whole-sheet сохраняется точный raw response; fragment-reader архивирует в одном журнале основные и дочерние chain/features/profile-вопросы с моделью и полным raw response.
- [x] Для каждого принятого scalar-значения хранить значение по проходам, голоса, confidence, evidence и номера проходов; для feature-параметров — stated/derived/assumed/human provenance.
- [x] Добавить API:
  - `GET /api/image-gen/{id}/cad-reading`;
  - `GET /api/image-gen/{id}/solid-input`;
  - `GET /api/image-gen/{id}/solid-input/diff`;
  - `PATCH /api/image-gen/{id}/cad-reading`;
  - `POST /api/image-gen/{id}/solid-input/rebuild`.
- [x] Добавить SHA-256 канонического payload, чтобы показанное пользователю было побайтно тем, что получил CAD-kernel.
- [x] В `/cad/{id}` добавить панели «Результат чтения», «Вход 3D-модели» и diff между ними.
- [x] Показывать исключённые значения и причину исключения; по evidence переводить bbox tile/overview в координаты полного листа и подсвечивать источник по клику.
- [x] Разрешить правку нормализованных значений до построения и повторную сборку без нового VLM-чтения; сохранять ревизии и автора изменения.

## P2. Evidence-first чтение и граф размеров

- [x] Разделить распознавание на обзор листа, виды/разрезы, локальные tiles и независимый OCR размерных надписей.
- [x] Ввести явную модель видов, линий разреза, вынесенных сечений и связей между проекциями. Spec хранит `view_id`, `parent_view_id`, relation, feature coverage, положение и model-space путь секущей; вынесенное сечение строится как отдельный B-Rep section. Местный detail строится нативным `TechDraw::DrawViewDetail` по доказанным model-space центру/радиусу и отдельному масштабу; неполный detail остаётся coverage-блокером.
- [-] Собирать граф размерных цепей: реализованы наружная/внутренняя длина с offset, габарит, накопительные координаты ступеней, осевое размещение features, конусность, локальный диаметр поперечного отверстия и координатное попадание отверстий/пазов в плоский профиль. Допуски нулевой/симметричной зоны `H/h/JS/js` теперь покрывают IT5–IT16, табличные поля `F6..F9`, `G6..G9`, `f6..f9`, `g6..g9`, `k6/m6/n6/p6` и явные `±` разворачиваются в min/max; прочие валидные поля сохраняются как явно неполный интервал без угадывания стороны. Радиус галтели проверяется по перепаду радиусов и длинам соседних ступеней. Для плоского прямоугольного профиля `corner_radius_mm` проходит сквозь schema, граф, 3D-kernel и 2D-дуги; граф проверяет предельный R и попадание отверстия в скруглённый угол. Остаётся расширить фундаментальные отклонения на остальные буквенные поля.
- [x] Решать реализованные размерные ограничения детерминированно; конфликт блокирует 3D-построение.
- [x] Запретить перенос размерной надписи с наружного контура во внутренний и обратно без доказанной связи.
- [x] Добавить targeted follow-up только для конкретного неизвестного параметра; ответ принимается лишь при совпадении с независимо прочитанной выноской, отсутствие выносок теперь fail-closed.

## P3. Точная 3D-модель

- [x] Компилировать поддержанный feature tree: revolve/extrude, bore/taper, holes, keyways/grooves, chamfers/fillets, threads.
- [x] Для каждой операции возвращать структурированный результат ядра `requested/built/failed` и причину отказа.
- [x] Проверять габариты, профильный верхний предел объёма с учётом bore, число solid/shell/face/edge/vertex, manifold/B-Rep и каждую feature-операцию. Для каждой геометрической операции ядро измеряет булеву разность B-Rep до/после, сохраняет фактические/ожидаемые объём и bbox; `built` без `localization_ok=true` блокирует верификацию.
- [x] Не откатывать неудавшийся feature молча к предыдущему «валидному» телу: отказ фиксируется как failed и блокирует геометрическую верификацию.
- [x] Добавить независимый рендер 3D и сравнение проекций с исходными видами/разрезами: spec full-check всегда передаёт модели исходный лист и текущий рендер результата в фиксированном порядке, сравнивает только геометрию и сохраняет парный отчёт на текущей ревизии.

## P4. Необходимые 2D-виды из проверенной 3D

- [x] Планировать минимальный достаточный набор видов по feature coverage, а не фиксированную тройку front/side/top.
- [x] Для тел вращения с внутренней геометрией строить продольный разрез со штриховкой.
- [x] Торцевой вид автоматически добавляется для радиальных features, пазов и осевых шаблонов отверстий; прямой/ступенчатый разрез передаёт `section_origin_mm/section_path_mm` в kernel, вынесенное сечение строится как B-Rep section с отдельным `presentation_kind=removed_section`, местный detail — как нативный crop родительской проекции с собственным масштабом; причина каждого вида показывается пользователю.
- [x] Сверять каждый прочитанный геометрический размер с реально размещённым размером; непомещённая выноска является блокером с явной причиной.
- [x] Исправить дублирование текста размеров: полный callout (`17`, `Ø56.55`, `M75x1,5`) является единственным отображаемым текстом и не дополняется измеренным числом повторно.
- [x] Сохранять допуски, посадки, шероховатости и базы как связанные семантические сущности: размер текущей B-Rep-проекции хранит точный текст, measured value и структурированные `fit/deviation/thread` в одной `DimensionEntity`; `roughness/datum/tolerance/thread/weld` представлены `AnnotationEntity`, а drawing graph — явными `annotation_applies_to`. Geometry-only компоновщик резервирует отдельную полосу только под производственные обозначения, сохраняет точное исходное написание и не переносит туда общие материал/твёрдость/прочие требования. Живой OpenCascade/TechDraw smoke разместил на одном листе `Ø80g6`, `Ra 1,6`, базу `Д` и рамку биения `↗ 0,008 Д`; все три annotation-сущности и пять размеров прошли geometry-only DXF semantic reopen (`20/20`).

## P5. DXF только с геометрией детали

- [x] Добавить режим экспорта `geometry_only=true`: без рамки и основной надписи.
- [x] Сохранить слои контуров, скрытых линий, осей, штриховки, размеров и аннотаций.
- [x] Перед сохранением production spec-ревизии DXF повторно открывается и семантически сверяется с CadIR по типам редактируемых сущностей и слоям; несовпадение создаёт блокирующую validation issue.

## P6. UX проверки и статусы

- [x] Перед повторным построением показывать блокеры, предположения, сохранённое чтение и точный 3D input; пустой rebuild не считается human-correction и не обходит evidence-gate.
- [x] Добавить пошаговое устранение source-blocker-ов прямо в сохранённом чтении: для осевых резьбовых отверстий редактируются торец, сквозность/глубина и подготовительный Ø; для фасок показываются заявленное/локализованное количество, кромка и координата по Z/Ø. Справочные M8 значения отделены от полей и никогда не подставляются автоматически. Неполную правку можно записать в audit/training pair, но rebuild остаётся заблокирован. Повторные правки накапливаются поверх предыдущей человеческой ревизии, не меняя исходный ответ модели.
- [x] После сборки показывать заказанные features, параметры/provenance, блокеры и статус; результаты ядра сохраняются в отчёте.
- [x] Не присваивать `verified` до независимой проверки исходных проекций; обычный успешный B-Rep имеет статус `built_unverified`.
- [x] Сравнение исходника и результата сделать основным review-потоком, а не отдельной необязательной проверкой PNG: без исходника full-check для `spec` не запускается, новая ревизия инвалидирует сверку, а human accept разрешён только после чистой парной проверки именно текущей ревизии; результат и замечания видны в панели проверки.

## P7. Тесты и promotion gate

- [x] Unit: покрыты critical missing data, dimension graph, topology/tolerance, feature completeness и B-Rep localization contract, labels, geometry-only, per-value provenance, source bbox mapping и view coverage.
- [x] Integration: отдельные контракты reader → normalized spec, byte-identical kernel payload, B-Rep → sheet и production DXF semantic reopen покрыты; production smoke дополнен единым реальным маршрутом normalized spec → feature tree → OpenCascade B-Rep → TechDraw-разрез → geometry-only CadIR → DXF → независимое semantic reopen с точным набором размерных выносок.
- [-] Regression: `detal_126.png` является первым hand-checked real-raster полым шпинделем и отдельным отрицательным кейсом неоднозначных фасок. Аудит прежнего `example-drawings/ground_truth.json` показал, что все его `7/7` листов имеют `source=synthetic`, поэтому они оставлены contract fixtures, но больше не могут изображать реальный regression-корпус: `eval_reader_fields.py` публикует `corpus.by_source/real_sheets`, требует явно маркированные real sources и завершает promotion ненулевым кодом при их отсутствии. Остаётся добавить независимо размеченные реальные вал, плиту/фланец и ещё неоднозначные листы; синтетика в этот счётчик не попадёт.
- [-] Метрики: reference spec v3 исправлен после повторной ручной сверки. Он проверяет `77` source-supported параметров: прежние группы плюс `Ø98×13`, измеренное по контуру смещение входа M8 `5,6 мм`, а также доказуемые поля массивов `8×Ø1/Ø65/45°` и `12×Ø4/Ø70/18/82`. Фаски исключены из числового score, пока ни одна кромка не локализована детерминированно; требование «6 фасок» целиком остаётся build-blocker. Неизвестные торец и угловая фаза двух массивов также не входят в score как выдуманная истина. Историческое `63/63` отозвано как критерий точности.
- [-] Live reader promotion: исторический прогон `63/63` отозван после визуального аудита: он ошибочно связал M8 с `Ø65`, пропустил `Ø98`, `8×Ø1` и `12×Ø4`, поэтому `parameter_accuracy=1.0` измеряла неполный неверный reference. Финальный production replay 2026-08-02 на `qwen3_6_35b_apex_ollama` занял `291,9 с` и достиг `77/77`, `parameter_accuracy=1.0`, `8/9` coarse facts, `false_accept=false`. Совпали наружный профиль `Ø102×14 + Ø98×13 + Ø80×344 + Ø72×6 + M75×1,5×18 + Ø72×75`, оба паза, расточка/резьбы/поперечные отверстия, M8 на PCD `Ø80` со входной плоскостью `5,6 мм`, `8×Ø1/Ø65/45°` и `12×Ø4/Ø70/18/82`. Полная сборка правильно заблокирована четырьмя source-причинами: неизвестен Ø входной выборки M8; неизвестны торец и угловая фаза двух массивов; не локализованы 6 фасок.
- [-] Production rebuild и финальный живой CLI-прогон `detal_126.png` выполнены на пересозданном backend и здоровом CAD-kernel. Полный STL/DXF намеренно не выпущен: build-gate перечисляет недоказанную геометрию и не подменяет её defaults. Отдельный production CAD-kernel smoke наклонного отверстия завершился HTTP `200`, архив артефактов `40 762` байта. Для окончательной полной модели остаётся пользовательская/источниковая локализация четырёх групп blocker-данных через редактор прочитанной спецификации.

## Definition of Done для `detal_126.png`

- [x] Общая длина `470 ± 0.05 мм`, максимальный диаметр `102 ± 0.05 мм`.
- [x] Совпадают все `77/77` доказуемых параметра исправленного эталона, включая `Ø98×13`, M8 на `Ø80` с измеренным входом `5,6 мм`, `8×Ø1/Ø65/45°` и `12×Ø4/Ø70/18/82`; все шесть фасок остаются отдельным blocker до локализации кромок.
- [ ] Все подтверждённые операции реально собраны CAD-ядром и прошли строгую semantic verification. Сейчас M8 не строятся без Ø входной выборки; массивы Ø1/Ø4 — без торца/фазы; шесть фасок — без локализованных кромок.
- [ ] Нет ни одной проваленной или молча пропущенной feature-операции.
- [ ] Лист содержит необходимые виды и продольный разрез со штриховкой; все нужные размеры размещены один раз.
- [ ] DXF экспортирован без рамки и штампа, повторно открыт и семантически совпадает с проверенной CadIR.
- [x] Пользователь видит полный diff «прочитано → передано в 3D → реально построено»: таблица принятых значений ведёт к source-bbox, точный kernel payload содержит SHA-256 и param provenance, третья колонка показывает сохранённый kernel report, выполненные feature-операции и verification. При блокировке явно написано, что ядро не вызывалось и реально построенных операций нет.
- [ ] Автоматический статус `verified` возможен только после всех перечисленных проверок; до этого — `blocked` или `built_unverified`.
